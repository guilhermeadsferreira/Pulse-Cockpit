---
phase: 999.1-resumo-1on1-estilo-qulture-rocks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/prompts/1on1-deep.prompt.ts
  - src/renderer/src/views/PersonView.tsx
autonomous: true
requirements:
  - QR-01
must_haves:
  truths:
    - "Ao expandir um artefato de 1:1 com Pass 2 processado, o gestor vê um bloco destacado com o resumo executivo para Qulture Rocks"
    - "O bloco exibe parágrafo de abertura, bullets de ações do liderado (com responsável), bullets de ações do gestor (com responsável), e bullets de próximos passos"
    - "O botão 'Copiar para QR' copia apenas o texto do resumo para o clipboard (não o artefato completo)"
    - "Artefatos de 1:1 sem a seção '## Resumo Executivo (Qulture Rocks)' não exibem o bloco nem o botão"
    - "Artefatos de tipos não-1:1 não exibem o bloco"
    - "O resumo gerado pelo Pass 2 omite tendência emocional, insights de carreira, PDI e dados sensíveis"
  artifacts:
    - path: "src/main/prompts/1on1-deep.prompt.ts"
      provides: "Prompt revisado com estrutura obrigatória D-03/D-04/D-05 para resumo_executivo_rh"
      contains: "Ações do liderado (com •)"
    - path: "src/renderer/src/views/PersonView.tsx"
      provides: "ArtifactCard com seção de resumo QR extraída do conteúdo do artefato"
      contains: "Resumo Executivo"
  key_links:
    - from: "src/main/prompts/1on1-deep.prompt.ts"
      to: "campo resumo_executivo_rh no JSON salvo pelo ArtifactWriter"
      via: "instrução do prompt que produz o campo"
      pattern: "resumo_executivo_rh"
    - from: "src/renderer/src/views/PersonView.tsx (ArtifactCard)"
      to: "conteúdo do artefato markdown carregado via window.api.artifacts.read"
      via: "regex extraindo texto após '## Resumo Executivo (Qulture Rocks)'"
      pattern: "Resumo Executivo \\(Qulture Rocks\\)"
---

<objective>
Refinar o prompt do Pass 2 (1on1-deep) para que o campo `resumo_executivo_rh` siga a estrutura D-03 com separação explícita de ações por responsável, e exibir esse resumo como seção destacada no ArtifactCard da PersonView com botão de cópia para o Qulture Rocks.

Purpose: O gestor precisa de uma ata limpa e pronta para colar no Qulture Rocks e enviar ao liderado — sem análise interna, sem jargão do app, formatada como ata formal de reunião.
Output: Prompt revisado + UI com bloco "Resumo Executivo (Qulture Rocks)" expandível e botão de cópia.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.1-resumo-1on1-estilo-qulture-rocks/999.1-CONTEXT.md

<interfaces>
<!-- Tipos relevantes. Não explorar o codebase — usar esses direto. -->

De src/renderer/src/types/ipc.ts:
```typescript
export interface ArtifactMeta {
  path:      string
  fileName:  string
  tipo:      string   // 'oneonone' | '1on1' | outros — verificar labelTipo
  date:      string
}
```

De src/main/prompts/1on1-deep.prompt.ts:
```typescript
export interface OneOnOneResult {
  // ... outros campos ...
  resumo_executivo_rh: string  // campo alvo do prompt
}
```

De src/renderer/src/views/PersonView.tsx (ArtifactCard — estado atual):
```typescript
// content: string lido via window.api.artifacts.read(a.path)
// Frontmatter YAML já removido na leitura:
//   raw.replace(/^---\n[\s\S]*?\n---\n\n?/, '').trim()
// Exibido via: <MarkdownPreview content={content} maxHeight={480} />

// a.tipo: classificado via labelTipo(a.tipo)
// Tipos de 1:1 são identificados por: a.tipo === '1on1' ou a.tipo === 'oneonone'
// Verificar a função labelTipo em src/renderer/src/lib/utils.ts para o valor exato
```

Estilos disponíveis (de styles no topo de PersonView.tsx):
```typescript
styles.btnIcon  // { background, border, cursor, display, alignItems, etc. — 24×24 }
// Tokens: var(--surface), var(--surface-2), var(--surface-3), var(--border), var(--border-subtle)
// var(--text-muted), var(--text-secondary), var(--text-primary), var(--accent)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refinar prompt resumo_executivo_rh no Pass 2</name>
  <files>src/main/prompts/1on1-deep.prompt.ts</files>
  <read_first>
    - src/main/prompts/1on1-deep.prompt.ts — ler completo para ver a instrução atual do campo `resumo_executivo_rh` (linha ~193-198) antes de alterar
  </read_first>
  <action>
Localizar a instrução do campo `**"resumo_executivo_rh"**` no prompt (atualmente em torno das linhas 193-198) e substituir pelo texto abaixo. NÃO alterar nenhuma outra parte do arquivo.

Substituir este bloco:
```
**"resumo_executivo_rh"** — Resumo para Qulture Rocks:
Formato pronto para colar numa ferramenta de RH. Estrutura:
- Parágrafo de abertura (2-3 frases): o que foi tratado, contexto geral
- Ações combinadas (bullets com •): compromissos do liderado e do gestor, com responsável explícito
- Próximos passos (bullets com •): o que será acompanhado até o próximo 1:1
Tom: profissional e objetivo. Sem jargão interno do app. NÃO inclua indicadores de saúde, sentimento, tendência emocional, ou insights sensíveis de carreira/pessoal — apenas o que é apropriado para registro formal de RH.
```

Por este (per D-03, D-04, D-05):
```
**"resumo_executivo_rh"** — Resumo executivo para Qulture Rocks:
Ata limpa, autônoma e pronta para colar no Qulture Rocks e compartilhar com o liderado. Deve ser legível por alguém que não esteve presente na reunião.

Estrutura obrigatória — use exatamente estas seções na ordem abaixo:

1. Parágrafo de abertura (2-3 frases): tópicos principais discutidos na reunião, sem análise interna.
2. **Ações do liderado** — bullets com •, uma linha por ação, formato: "• [Nome do liderado]: [ação concreta]". Incluir apenas ações novas surgidas neste 1:1; omitir follow-ups de ciclos anteriores.
3. **Ações do gestor** — bullets com •, uma linha por ação, formato: "• [Nome do gestor]: [ação concreta]". Incluir apenas ações que o gestor se comprometeu explicitamente.
4. **Próximos passos** — bullets com •: temas ou decisões que serão acompanhados no próximo 1:1.

Tom: profissional, direto, sem jargão interno do app.
OMITIR obrigatoriamente: tendência emocional, insights de carreira/PDI, análise qualitativa interna, sinais sensíveis, saúde emocional — nada que seja inadequado para registro formal de RH ou para o liderado ler.
Se não houver ações do liderado ou do gestor neste 1:1, omitir a seção correspondente (não gerar bullets vazios).
```
  </action>
  <verify>
    <automated>grep -n "Ações do liderado" /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit/src/main/prompts/1on1-deep.prompt.ts && grep -n "Ações do gestor" /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit/src/main/prompts/1on1-deep.prompt.ts && grep -n "Próximos passos" /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit/src/main/prompts/1on1-deep.prompt.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/main/prompts/1on1-deep.prompt.ts contém a string "Ações do liderado"
    - src/main/prompts/1on1-deep.prompt.ts contém a string "Ações do gestor"
    - src/main/prompts/1on1-deep.prompt.ts contém a string "OMITIR obrigatoriamente"
    - src/main/prompts/1on1-deep.prompt.ts NÃO contém a string "Ações combinadas (bullets com •): compromissos do liderado e do gestor" (texto antigo removido)
    - O arquivo ainda exporta `build1on1DeepPrompt` e `OneOnOneResult` sem erros de sintaxe TypeScript (npx tsc --noEmit não retorna erros em src/main/prompts/1on1-deep.prompt.ts)
  </acceptance_criteria>
  <done>O prompt do Pass 2 instrui Claude a produzir resumo_executivo_rh com seções separadas por responsável (liderado / gestor / próximos passos) e sem dados sensíveis.</done>
</task>

<task type="auto">
  <name>Task 2: Exibir bloco QR no ArtifactCard + botão Copiar</name>
  <files>src/renderer/src/views/PersonView.tsx</files>
  <read_first>
    - src/renderer/src/views/PersonView.tsx — ler completo (especialmente ArtifactCard, linhas ~424-499, e a função labelTipo no início do arquivo ou verificar import de lib/utils) para entender o estado atual antes de alterar
    - src/renderer/src/lib/utils.ts — verificar o valor exato retornado por labelTipo para artefatos de 1:1 (para saber qual string comparar em a.tipo)
  </read_first>
  <action>
**Objetivo:** Dentro do `ArtifactCard`, quando o card é expandido e o artefato é do tipo 1:1 e o conteúdo contém a seção `## Resumo Executivo (Qulture Rocks)`, exibir um bloco destacado com o texto do resumo ACIMA do MarkdownPreview, com um botão "Copiar para QR".

**Passo 1 — Adicionar estado para feedback de cópia:**
No componente `ArtifactCard`, adicionar um novo estado:
```typescript
const [copied, setCopied] = useState(false)
```

**Passo 2 — Adicionar função de extração de resumo:**
Criar uma função helper fora do componente (no escopo do módulo, após os outros helpers):
```typescript
function extractQRSummary(content: string): string | null {
  const match = content.match(/##\s*Resumo Executivo \(Qulture Rocks\)([\s\S]*?)(?=\n##\s|\s*$)/)
  return match ? match[1].trim() : null
}
```

**Passo 3 — Adicionar função de cópia dentro do ArtifactCard:**
```typescript
async function copyQR(text: string, e: React.MouseEvent) {
  e.stopPropagation()
  await navigator.clipboard.writeText(text)
  setCopied(true)
  setTimeout(() => setCopied(false), 2000)
}
```

**Passo 4 — Determinar se artefato é 1:1:**
Na seção de conteúdo expandido, antes do MarkdownPreview, determinar se o artefato é 1:1:
```typescript
// Verificar o valor exato: inspecionar labelTipo para saber se a.tipo === '1on1'
// Pela análise do código, a.tipo vem do campo 'tipo' do ArtifactMeta
// Artefatos de 1:1 têm tipo '1on1' (verificar via labelTipo em utils.ts)
const is1on1 = a.tipo === '1on1'
const qrSummary = is1on1 && content ? extractQRSummary(content) : null
```

**Passo 5 — Renderizar bloco QR (ACIMA do MarkdownPreview):**
Dentro do bloco `{expanded && content !== null && (...)}`, na `<div>` de padding, adicionar o bloco QR ANTES do `<MarkdownPreview>`:

```tsx
{qrSummary && (
  <div style={{
    background: 'var(--surface-2)',
    border: '1px solid var(--border)',
    borderRadius: 6,
    padding: '12px 14px',
    marginBottom: 14,
  }}>
    <div style={{
      display: 'flex', alignItems: 'center', justifyContent: 'space-between',
      marginBottom: 8,
    }}>
      <span style={{
        fontSize: 10, fontWeight: 600, letterSpacing: '0.06em',
        textTransform: 'uppercase' as const,
        color: 'var(--text-muted)',
      }}>
        Resumo Executivo (Qulture Rocks)
      </span>
      <button
        onClick={(e) => copyQR(qrSummary, e)}
        style={{
          ...styles.btnIcon,
          fontSize: 11, padding: '3px 8px', width: 'auto', height: 'auto',
          display: 'inline-flex', alignItems: 'center', gap: 4,
          color: copied ? 'var(--accent)' : 'var(--text-muted)',
          border: '1px solid var(--border)',
          borderRadius: 4, background: 'var(--surface-3)',
        }}
        title="Copiar resumo para o Qulture Rocks"
      >
        {copied ? 'Copiado!' : 'Copiar para QR'}
      </button>
    </div>
    <pre style={{
      margin: 0, whiteSpace: 'pre-wrap', fontSize: 12,
      lineHeight: 1.6, color: 'var(--text-secondary)',
      fontFamily: 'var(--font)',
    }}>
      {qrSummary}
    </pre>
  </div>
)}
```

**Passo 6 — Adicionar import de `Copy` do lucide-react (se necessário para ícone):**
Não é necessário — o botão usa texto. Nenhum novo import de ícone obrigatório.

**IMPORTANTE — restrições:**
- Não alterar a lógica de `toggle()` nem a leitura do conteúdo do artefato
- Não alterar nenhum outro componente além de `ArtifactCard`
- Não adicionar novos campos ao `ArtifactMeta` — usar `a.tipo` que já existe
- Se `labelTipo` em utils.ts revelar que o tipo exato é diferente de `'1on1'` (ex: `'oneonone'`), usar o valor correto encontrado via leitura de utils.ts
  </action>
  <verify>
    <automated>grep -n "extractQRSummary" /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit/src/renderer/src/views/PersonView.tsx && grep -n "Copiar para QR" /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit/src/renderer/src/views/PersonView.tsx && grep -n "copyQR" /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit/src/renderer/src/views/PersonView.tsx</automated>
  </verify>
  <acceptance_criteria>
    - PersonView.tsx contém a função `extractQRSummary` com regex `Resumo Executivo \(Qulture Rocks\)`
    - PersonView.tsx contém a string `"Copiar para QR"`
    - PersonView.tsx contém a string `copyQR`
    - PersonView.tsx contém a string `navigator.clipboard.writeText`
    - PersonView.tsx contém `setCopied(true)` e `setCopied(false)` (feedback visual de cópia)
    - PersonView.tsx contém a string `Resumo Executivo (Qulture Rocks)` dentro do JSX do bloco destacado
    - A verificação `is1on1` condiciona a exibição do bloco (o bloco não aparece para outros tipos de artefato)
    - O bloco QR está posicionado ANTES do `<MarkdownPreview` no JSX (verificar ordem no arquivo)
  </acceptance_criteria>
  <done>
    Ao expandir um artefato de 1:1 com resumo QR presente, o gestor vê o bloco destacado acima do conteúdo completo. O botão "Copiar para QR" mostra "Copiado!" por 2 segundos após copiar. Para artefatos sem o resumo ou de outros tipos, nenhum bloco é exibido.
  </done>
</task>

</tasks>

<verification>
**Verificação manual após execução:**

1. Abrir PersonView de um liderado com artefato de 1:1 processado pelo Pass 2 (que já tem `## Resumo Executivo (Qulture Rocks)` no arquivo)
   - Clicar no card para expandir → bloco destacado aparece acima do markdown completo
   - Clicar "Copiar para QR" → botão mostra "Copiado!" por ~2 segundos
   - Colar em um editor de texto → conteúdo é apenas o texto do resumo, não o artefato inteiro

2. Abrir PersonView de um liderado com artefato de 1:1 ingerido antes do Pass 2 (sem o campo)
   - Expandir o card → bloco QR não aparece; apenas MarkdownPreview normal

3. Abrir PersonView com artefato de tipo diferente (ex: reunião coletiva)
   - Expandir o card → bloco QR não aparece

4. Para o próximo 1:1 ingerido após a mudança de prompt:
   - Verificar no arquivo gerado que `## Resumo Executivo (Qulture Rocks)` tem seções separadas: "Ações do liderado", "Ações do gestor", "Próximos passos"
   - Verificar que não contém palavras como "tendência emocional", "PDI" ou "saúde"
</verification>

<success_criteria>
- `extractQRSummary` existe em PersonView.tsx e usa regex `/##\s*Resumo Executivo \(Qulture Rocks\)/`
- Bloco QR visível apenas em artefatos 1:1 com campo presente
- Botão copia apenas o texto do resumo via `navigator.clipboard.writeText`
- Prompt em 1on1-deep.prompt.ts instrui separação de ações por responsável com bullets e `•`
- Prompt contém "OMITIR obrigatoriamente" com lista de campos sensíveis
- Zero alterações em ArtifactWriter, IngestionPipeline, SchemaValidator ou outros arquivos fora do escopo
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/999.1-resumo-1on1-estilo-qulture-rocks/999.1-01-SUMMARY.md`
</output>
