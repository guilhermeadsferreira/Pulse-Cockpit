---
phase: 999.11-sustentacao-trending-sla-compliance
plan: "02"
type: execute
wave: 2
depends_on:
  - "999.11-01"
files_modified:
  - src/main/index.ts
autonomous: true
requirements:
  - STRD-03

must_haves:
  truths:
    - "A cada fetch real (não cache hit), um entry é gravado em history.json com deduplicação diária"
    - "history.json nunca ultrapassa 90 dias de entradas — entradas mais antigas são removidas automaticamente"
    - "Falha ao gravar history.json não impede o retorno do snapshot ao renderer"
    - "sustentacao:get-data retorna SupportBoardSnapshot com history[] populado (últimos 30 snapshots)"
    - "Cache hit (TTL 1h) retorna snapshot com history[] populado sem disparar novo fetch"
  artifacts:
    - path: "src/main/index.ts"
      provides: "fetchAndCacheSustentacao com gravação de history.json + history[] no retorno"
      contains: "history.json"
  key_links:
    - from: "src/main/index.ts fetchAndCacheSustentacao"
      to: "~/.pulsecockpit/cache/sustentacao/history.json"
      via: "writeFileSync após gravar board.json"
      pattern: "history.json"
    - from: "src/main/index.ts fetchAndCacheSustentacao"
      to: "SupportBoardSnapshot.history"
      via: "readHistory + spread no return"
      pattern: "history: historyData"
---

<objective>
Implementar acumulação de snapshots históricos em history.json e incluir os últimos 30 snapshots no payload retornado por sustentacao:get-data.

Purpose: Sem este plano, o renderer não tem série temporal para calcular deltas e renderizar mini charts. O history.json é o pré-requisito para fases 999.12-999.14.
Output: fetchAndCacheSustentacao() grava history.json a cada fetch real, retorna snapshot com history[] preenchido.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.11-sustentacao-trending-sla-compliance/999.11-CONTEXT.md
@.planning/phases/999.11-sustentacao-trending-sla-compliance/999.11-RESEARCH.md
@.planning/phases/999.11-sustentacao-trending-sla-compliance/999.11-01-SUMMARY.md
</context>

<interfaces>
<!-- Contexto crítico para o executor -->

De src/main/index.ts (linhas 1115-1153) — fetchAndCacheSustentacao atual:
```typescript
async function fetchAndCacheSustentacao(): Promise<SupportBoardSnapshot | null> {
  const settings = SettingsManager.load()
  const { jiraSupportProjectKey, jiraBaseUrl, jiraEmail, jiraApiToken, jiraSlaThresholds } = settings

  if (!jiraSupportProjectKey || !jiraBaseUrl || !jiraEmail || !jiraApiToken) {
    return null
  }

  const cacheDir = join(settings.workspacePath, '..', 'cache', 'sustentacao')
  const cacheFile = join(cacheDir, 'board.json')
  const CACHE_TTL_MS = 60 * 60 * 1000

  try {
    if (existsSync(cacheFile)) {
      const cached = JSON.parse(readFileSync(cacheFile, 'utf-8')) as { data: SupportBoardSnapshot; fetchedAt: number }
      if (Date.now() - cached.fetchedAt < CACHE_TTL_MS) {
        // cache hit — return here
        return cached.data
      }
    }
  } catch { /* cache inválido — refetch */ }

  // ... fetch real via fetchSupportBoardMetrics ...
  const data = await fetchSupportBoardMetrics({ ... })

  try {
    if (!existsSync(cacheDir)) mkdirSync(cacheDir, { recursive: true })
    writeFileSync(cacheFile, JSON.stringify({ data, fetchedAt: Date.now() }), 'utf-8')
  } catch (err) { ... }

  return data
}
```

Novo tipo (criado no Plan 01):
```typescript
export interface SustentacaoHistoryEntry {
  date: string          // YYYY-MM-DD
  fetchedAt: number
  ticketsAbertos: number
  ticketsFechadosUltimos30d: number
  breachCount: number
  complianceRate7d: number | null
  complianceRate30d: number | null
}
// SupportBoardSnapshot agora tem: complianceRate7d, complianceRate30d, history: SustentacaoHistoryEntry[]
```

Imports disponíveis no topo de index.ts: existsSync, readFileSync, writeFileSync, mkdirSync já importados de 'fs'.
join já importado de 'path'.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Implementar history.json em fetchAndCacheSustentacao</name>
  <files>src/main/index.ts</files>

  <read_first>
    - src/main/index.ts (linhas 1114-1200) — ler a função fetchAndCacheSustentacao completa e os handlers que a usam
    - src/renderer/src/types/ipc.ts — confirmar que SustentacaoHistoryEntry e os novos campos de SupportBoardSnapshot existem (criados no Plan 01)
  </read_first>

  <action>
**Localizar `fetchAndCacheSustentacao` em src/main/index.ts e fazer as seguintes modificações:**

**1. Adicionar função helper `readHistory` antes de `fetchAndCacheSustentacao`:**

```typescript
function readHistory(historyFile: string): SustentacaoHistoryEntry[] {
  try {
    if (!existsSync(historyFile)) return []
    return JSON.parse(readFileSync(historyFile, 'utf-8')) as SustentacaoHistoryEntry[]
  } catch {
    // history.json corrompido ou inválido — retornar vazio sem propagar erro
    return []
  }
}
```

Garantir que `SustentacaoHistoryEntry` está nos imports vindos de `'../../renderer/src/types/ipc'` (ou o caminho correto relativo a index.ts). Adicionar ao import existente se necessário.

**2. Modificar o branch de cache hit** para incluir history no retorno:

Onde está `return cached.data` (cache hit), substituir por:

```typescript
        Logger.getInstance().child('IPC').info('sustentacao:get-data cache hit')
        const historyFile = join(cacheDir, 'history.json')
        const historyData = readHistory(historyFile).slice(-30)
        return { ...cached.data, history: historyData }
```

**3. Após `writeFileSync(cacheFile, ...)` (gravação do board.json), adicionar bloco de gravação do history.json:**

```typescript
    // Gravar snapshot no histórico diário (history.json)
    try {
      const historyFile = join(cacheDir, 'history.json')
      const dateKey = new Date().toISOString().slice(0, 10) // YYYY-MM-DD

      const history = readHistory(historyFile)

      // Deduplica: remover entrada do mesmo dia (mantém apenas a mais recente)
      const filtered = history.filter((e) => e.date !== dateKey)

      // Append entrada de hoje (usar apenas campos agregados — sem ticketsEmBreach completo)
      filtered.push({
        date: dateKey,
        fetchedAt: Date.now(),
        ticketsAbertos: data.ticketsAbertos,
        ticketsFechadosUltimos30d: data.ticketsFechadosUltimos30d,
        breachCount: data.ticketsEmBreach.length,
        complianceRate7d: data.complianceRate7d,
        complianceRate30d: data.complianceRate30d,
      })

      // Retenção: 90 dias
      const cutoff = Date.now() - 90 * 24 * 60 * 60 * 1000
      const retained = filtered.filter((e) => e.fetchedAt >= cutoff)

      writeFileSync(historyFile, JSON.stringify(retained), 'utf-8')
    } catch (histErr) {
      Logger.getInstance().child('IPC').warn('sustentacao: falha ao gravar history (graceful)', {
        error: histErr instanceof Error ? histErr.message : String(histErr),
      })
    }
```

**4. Modificar o `return data` final** para incluir history:

```typescript
    const historyFile = join(cacheDir, 'history.json')
    const historyData = readHistory(historyFile).slice(-30)
    return { ...data, history: historyData }
```

**Pontos críticos:**
- `readHistory` deve ser chamada DEPOIS de gravar o history.json atualizado, para que o snapshot de hoje já esteja incluído nos dados retornados
- O try/catch do history é separado do try/catch do board.json — falha no history não impede retorno
- `SustentacaoHistoryEntry` deve ser importado junto com `SupportBoardSnapshot` no import existente
  </action>

  <verify>
    <automated>cd /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit && grep -n "history.json" src/main/index.ts && grep -n "readHistory" src/main/index.ts && grep -n "history: historyData" src/main/index.ts && grep -n "90 \* 24" src/main/index.ts</automated>
  </verify>

  <acceptance_criteria>
    - index.ts contém função `function readHistory(historyFile: string): SustentacaoHistoryEntry[]`
    - index.ts contém `const historyFile = join(cacheDir, 'history.json')` pelo menos 2 vezes (cache hit + fetch real)
    - index.ts contém `const retained = filtered.filter((e) => e.fetchedAt >= cutoff)` para retenção de 90 dias
    - index.ts contém `history: historyData` no return tanto do branch cache hit quanto do branch fetch real
    - index.ts contém o bloco try/catch separado para gravação do history com mensagem de warn graceful
    - `SustentacaoHistoryEntry` aparece no import de ipc.ts no topo do arquivo
  </acceptance_criteria>

  <done>fetchAndCacheSustentacao() acumula history.json com dedup diária, retenção de 90 dias, e retorna snapshot com history[] (últimos 30) em todos os branches (cache hit e fetch real).</done>
</task>

</tasks>

<verification>
Após completar a task:
1. `grep -c "history.json" src/main/index.ts` — deve retornar >= 3 (definição da path, gravação, leitura)
2. `grep -n "readHistory" src/main/index.ts` — deve aparecer: definição + 2 chamadas (cache hit + fetch real)
3. `grep -n "90 \* 24 \* 60 \* 60 \* 1000" src/main/index.ts` — deve existir o cutoff de 90 dias
4. TypeScript não reporta erros novos em index.ts
</verification>

<success_criteria>
- history.json é gravado a cada fetch real com deduplicação por data (YYYY-MM-DD)
- Entradas com fetchedAt mais de 90 dias são removidas automaticamente
- Falha no history nunca propaga para o retorno do snapshot
- sustentacao:get-data retorna SupportBoardSnapshot com history[] preenchido
- Cache hit também retorna history[] (lê do disco sem refetch)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/999.11-sustentacao-trending-sla-compliance/999.11-02-SUMMARY.md`
</output>
