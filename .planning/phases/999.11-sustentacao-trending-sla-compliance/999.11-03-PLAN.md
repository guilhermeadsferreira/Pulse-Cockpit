---
phase: 999.11-sustentacao-trending-sla-compliance
plan: "03"
type: execute
wave: 3
depends_on:
  - "999.11-01"
  - "999.11-02"
files_modified:
  - src/renderer/src/views/SustentacaoView.tsx
autonomous: true
requirements:
  - STRD-04
  - STRD-05

must_haves:
  truths:
    - "SustentacaoView exibe dois novos cards: SLA Compliance 7d e SLA Compliance 30d com percentual (ou '—' quando null)"
    - "Os cards existentes (Tickets Abertos, Fechados 30d, Em Breach) exibem delta vs 7 dias atrás (↑/↓/= + número) ou '—' quando sem histórico"
    - "Mini chart SVG de breach count (últimos 30 dias) é exibido abaixo dos cards"
    - "Mini chart SVG de compliance % (últimos 30 dias) é exibido ao lado do chart de breach"
    - "Quando history[] está vazio (primeiros dias), a UI exibe '—' para deltas — sem crash"
  artifacts:
    - path: "src/renderer/src/views/SustentacaoView.tsx"
      provides: "UI com compliance cards, delta indicators e mini SVG charts"
      contains: "MiniLineChart"
  key_links:
    - from: "SustentacaoView"
      to: "snapshot.history"
      via: "getDelta() e buildChartPoints() helpers locais"
      pattern: "snapshot.history"
    - from: "MiniLineChart"
      to: "SVG polyline"
      via: "coords calculados via normalização min-max"
      pattern: "polyline"
---

<objective>
Atualizar SustentacaoView para exibir compliance cards, delta indicators nos cards existentes e mini charts SVG de evolução temporal.

Purpose: Transforma o módulo de sustentação de uma fotografia estática em indicador de tendência — o gestor vê em segundos se está melhorando ou piorando.
Output: SustentacaoView com compliance cards, deltas e 2 mini line charts SVG (breach + compliance).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.11-sustentacao-trending-sla-compliance/999.11-CONTEXT.md
@.planning/phases/999.11-sustentacao-trending-sla-compliance/999.11-RESEARCH.md
@.planning/phases/999.11-sustentacao-trending-sla-compliance/999.11-01-SUMMARY.md
@.planning/phases/999.11-sustentacao-trending-sla-compliance/999.11-02-SUMMARY.md
</context>

<interfaces>
<!-- Contexto para o executor — ler antes de tocar no arquivo -->

SupportBoardSnapshot estendido (após Plan 01 + 02):
```typescript
interface SupportBoardSnapshot {
  atualizadoEm: string
  ticketsAbertos: number
  ticketsFechadosUltimos30d: number
  topTipos: Array<{ tipo: string; count: number }>
  topLabels: Array<{ label: string; count: number }>
  ticketsEmBreach: SupportTicket[]
  porAssignee: Record<string, number>
  complianceRate7d: number | null   // null = sem tickets resolvidos na janela
  complianceRate30d: number | null
  history: SustentacaoHistoryEntry[]  // últimos 30 snapshots diários
}

interface SustentacaoHistoryEntry {
  date: string          // YYYY-MM-DD
  fetchedAt: number
  ticketsAbertos: number
  ticketsFechadosUltimos30d: number
  breachCount: number
  complianceRate7d: number | null
  complianceRate30d: number | null
}
```

SustentacaoView atual: usa `snapshot: SupportBoardSnapshot | null`, chama `window.api.sustentacao.getData()`.
MetricCard componente local na view: `function MetricCard({ label, value, highlight })`.
Section componente local: `function Section({ title, children })`.
Estilos inline (padrão do projeto — sem classes CSS).
CSS variables disponíveis: `var(--accent)`, `var(--red)`, `var(--red-dim)`, `var(--surface)`, `var(--surface-2)`, `var(--border)`, `var(--text-primary)`, `var(--text-secondary)`, `var(--text-muted)`, `var(--font)`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar helpers, compliance cards, deltas e mini charts na SustentacaoView</name>
  <files>src/renderer/src/views/SustentacaoView.tsx</files>

  <read_first>
    - src/renderer/src/views/SustentacaoView.tsx — ler arquivo completo para ver estrutura atual dos cards, MetricCard, Section, e onde inserir as mudanças
    - src/renderer/src/types/ipc.ts — confirmar que SustentacaoHistoryEntry está exportado e SupportBoardSnapshot tem os novos campos
  </read_first>

  <action>
**1. Adicionar import de SustentacaoHistoryEntry** no topo do arquivo (junto ao import existente de SupportBoardSnapshot):

```typescript
import type { SupportBoardSnapshot, SustentacaoHistoryEntry } from '../types/ipc'
```

**2. Adicionar funções helper locais** antes da função `SustentacaoView` (após os imports):

```typescript
/** Retorna delta absoluto vs snapshot de ~7 dias atrás. null se não há referência. */
function getDelta(current: number, history: SustentacaoHistoryEntry[], field: keyof Pick<SustentacaoHistoryEntry, 'ticketsAbertos' | 'ticketsFechadosUltimos30d' | 'breachCount'>): number | null {
  const sevenDaysMs = 7 * 24 * 60 * 60 * 1000
  const ref = history
    .filter((e) => e.fetchedAt <= Date.now() - sevenDaysMs)
    .sort((a, b) => b.fetchedAt - a.fetchedAt)[0]
  if (!ref) return null
  return current - (ref[field] as number)
}

/** Formata delta para exibição: "↑ 3", "↓ 2", "=" */
function formatDelta(delta: number | null): string {
  if (delta === null) return '—'
  if (delta === 0) return '='
  return delta > 0 ? `↑ ${delta}` : `↓ ${Math.abs(delta)}`
}

/** Cor do delta baseada no campo e direção. Para breach: subir é ruim. Para fechados: subir é bom. */
function deltaColor(delta: number | null, higherIsBad = false): string {
  if (delta === null || delta === 0) return 'var(--text-muted)'
  const isPositive = delta > 0
  const isGood = higherIsBad ? !isPositive : isPositive
  return isGood ? 'var(--accent)' : 'var(--red)'
}

/** Constrói array de pontos para um campo do histórico (últimos N dias). */
function buildChartPoints(history: SustentacaoHistoryEntry[], field: keyof Pick<SustentacaoHistoryEntry, 'breachCount' | 'complianceRate7d' | 'complianceRate30d'>): number[] {
  return history
    .slice(-30)
    .map((e) => (e[field] as number | null) ?? 0)
}
```

**3. Adicionar componente `MiniLineChart`** após os helpers (antes de `SustentacaoView`):

```typescript
function MiniLineChart({
  points,
  width = 180,
  height = 36,
  color = 'var(--accent)',
}: {
  points: number[]
  width?: number
  height?: number
  color?: string
}) {
  if (points.length < 2) return null
  const min = Math.min(...points)
  const max = Math.max(...points)
  const range = max - min || 1

  const coords = points.map((v, i) => {
    const x = (i / (points.length - 1)) * width
    const y = height - ((v - min) / range) * (height - 4) - 2 // 2px padding top/bottom
    return `${x.toFixed(1)},${y.toFixed(1)}`
  })

  return (
    <svg width={width} height={height} style={{ overflow: 'visible', display: 'block' }}>
      <polyline
        points={coords.join(' ')}
        fill="none"
        stroke={color}
        strokeWidth={1.5}
        strokeLinejoin="round"
        strokeLinecap="round"
      />
    </svg>
  )
}
```

**4. Modificar o bloco de cards de métricas sumárias** (onde estão os 3 MetricCard existentes).

Localizar o bloco atual (aproximadamente):
```jsx
<div style={{ display: 'flex', gap: 16, marginBottom: 28 }}>
  <MetricCard label="Tickets Abertos" value={snapshot.ticketsAbertos} />
  <MetricCard label="Fechados (30d)" value={snapshot.ticketsFechadosUltimos30d} />
  <MetricCard label="Em Breach de SLA" value={breachCount} highlight={breachCount > 0} />
</div>
```

Substituir por bloco expandido com compliance cards e deltas. Declarar as variáveis de delta antes do JSX, dentro do escopo do componente (após `const breachCount = snapshot.ticketsEmBreach.length`):

```typescript
  const deltaAbertos = getDelta(snapshot.ticketsAbertos, snapshot.history, 'ticketsAbertos')
  const deltaFechados = getDelta(snapshot.ticketsFechadosUltimos30d, snapshot.history, 'ticketsFechadosUltimos30d')
  const deltaBreach = getDelta(breachCount, snapshot.history, 'breachCount')
```

Novo bloco JSX dos cards (substituir o div de 3 MetricCard):

```jsx
        {/* Row 1: Compliance cards (novos) */}
        <div style={{ display: 'flex', gap: 16, marginBottom: 12 }}>
          <div style={{
            flex: 1, background: 'var(--surface)', border: '1px solid var(--border)',
            borderRadius: 8, padding: '16px 20px',
          }}>
            <div style={{ fontSize: 11, color: 'var(--text-muted)', fontFamily: 'var(--font)', marginBottom: 6, textTransform: 'uppercase', letterSpacing: '0.06em' }}>
              SLA Compliance 7d
            </div>
            <div style={{ fontSize: 28, fontWeight: 700, color: 'var(--text-primary)', fontFamily: 'var(--font)', lineHeight: 1 }}>
              {snapshot.complianceRate7d !== null ? `${snapshot.complianceRate7d}%` : '—'}
            </div>
          </div>
          <div style={{
            flex: 1, background: 'var(--surface)', border: '1px solid var(--border)',
            borderRadius: 8, padding: '16px 20px',
          }}>
            <div style={{ fontSize: 11, color: 'var(--text-muted)', fontFamily: 'var(--font)', marginBottom: 6, textTransform: 'uppercase', letterSpacing: '0.06em' }}>
              SLA Compliance 30d
            </div>
            <div style={{ fontSize: 28, fontWeight: 700, color: 'var(--text-primary)', fontFamily: 'var(--font)', lineHeight: 1 }}>
              {snapshot.complianceRate30d !== null ? `${snapshot.complianceRate30d}%` : '—'}
            </div>
          </div>
        </div>

        {/* Row 2: Cards existentes com deltas */}
        <div style={{ display: 'flex', gap: 16, marginBottom: 28 }}>
          <MetricCard
            label="Tickets Abertos"
            value={snapshot.ticketsAbertos}
            delta={formatDelta(deltaAbertos)}
            deltaColor={deltaColor(deltaAbertos, true)}
          />
          <MetricCard
            label="Fechados (30d)"
            value={snapshot.ticketsFechadosUltimos30d}
            delta={formatDelta(deltaFechados)}
            deltaColor={deltaColor(deltaFechados, false)}
          />
          <MetricCard
            label="Em Breach de SLA"
            value={breachCount}
            highlight={breachCount > 0}
            delta={formatDelta(deltaBreach)}
            deltaColor={deltaColor(deltaBreach, true)}
          />
        </div>
```

**5. Atualizar o componente `MetricCard`** para aceitar `delta` e `deltaColor` opcionais:

Localizar a definição de `MetricCard` no arquivo. Adicionar props opcionais e renderizar delta abaixo do value:

```typescript
function MetricCard({
  label,
  value,
  highlight = false,
  delta,
  deltaColor: deltaColorProp,
}: {
  label: string
  value: number
  highlight?: boolean
  delta?: string
  deltaColor?: string
}) {
  return (
    <div style={{
      flex: 1,
      background: 'var(--surface)',
      border: `1px solid ${highlight ? 'rgba(184,64,64,0.4)' : 'var(--border)'}`,
      borderRadius: 8,
      padding: '16px 20px',
    }}>
      <div style={{ fontSize: 11, color: 'var(--text-muted)', fontFamily: 'var(--font)', marginBottom: 6, textTransform: 'uppercase', letterSpacing: '0.06em' }}>
        {label}
      </div>
      <div style={{ display: 'flex', alignItems: 'baseline', gap: 8 }}>
        <div style={{ fontSize: 28, fontWeight: 700, color: highlight ? 'var(--red)' : 'var(--text-primary)', fontFamily: 'var(--font)', lineHeight: 1 }}>
          {value}
        </div>
        {delta && delta !== '—' && (
          <div style={{ fontSize: 12, color: deltaColorProp ?? 'var(--text-muted)', fontFamily: 'var(--font)' }}>
            {delta}
          </div>
        )}
      </div>
    </div>
  )
}
```

**6. Adicionar seção de Mini Charts** após os cards existentes e antes da seção "Análise de IA".

Antes do bloco `{analysisResult && (`, inserir:

```jsx
        {/* Mini Charts de evolução */}
        {snapshot.history.length >= 2 && (
          <Section title="Evolução (últimos 30 dias)">
            <div style={{ display: 'flex', gap: 32, flexWrap: 'wrap' }}>
              <div>
                <div style={{ fontSize: 11, color: 'var(--text-muted)', fontFamily: 'var(--font)', marginBottom: 8, textTransform: 'uppercase', letterSpacing: '0.06em' }}>
                  Breach Count
                </div>
                <MiniLineChart
                  points={buildChartPoints(snapshot.history, 'breachCount')}
                  color="var(--red)"
                />
              </div>
              <div>
                <div style={{ fontSize: 11, color: 'var(--text-muted)', fontFamily: 'var(--font)', marginBottom: 8, textTransform: 'uppercase', letterSpacing: '0.06em' }}>
                  Compliance 7d (%)
                </div>
                <MiniLineChart
                  points={buildChartPoints(snapshot.history, 'complianceRate7d')}
                  color="var(--accent)"
                />
              </div>
            </div>
          </Section>
        )}
```

**Nota:** A seção de charts só aparece quando `snapshot.history.length >= 2` — no primeiro dia de uso não aparece, sem "—" ou estado de loading.
  </action>

  <verify>
    <automated>cd /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit && grep -n "MiniLineChart" src/renderer/src/views/SustentacaoView.tsx && grep -n "getDelta" src/renderer/src/views/SustentacaoView.tsx && grep -n "complianceRate7d" src/renderer/src/views/SustentacaoView.tsx && grep -n "SLA Compliance" src/renderer/src/views/SustentacaoView.tsx && grep -n "polyline" src/renderer/src/views/SustentacaoView.tsx</automated>
  </verify>

  <acceptance_criteria>
    - SustentacaoView.tsx contém função `function MiniLineChart(` com `<polyline`
    - SustentacaoView.tsx contém função `function getDelta(`
    - SustentacaoView.tsx contém função `function formatDelta(`
    - SustentacaoView.tsx contém `SLA Compliance 7d` e `SLA Compliance 30d` como labels de card
    - SustentacaoView.tsx contém `snapshot.complianceRate7d` e `snapshot.complianceRate30d` com verificação de null
    - SustentacaoView.tsx contém `snapshot.history.length >= 2` como guard para os charts
    - SustentacaoView.tsx contém `buildChartPoints(snapshot.history, 'breachCount')` e `buildChartPoints(snapshot.history, 'complianceRate7d')`
    - MetricCard aceita props `delta` e `deltaColor` (grep: `delta?: string`)
  </acceptance_criteria>

  <done>SustentacaoView exibe compliance cards (7d e 30d), deltas nos cards existentes (↑/↓/=) e mini charts SVG de breach e compliance. '—' exibido quando sem dados históricos.</done>
</task>

</tasks>

<verification>
Após completar a task:
1. `grep -c "polyline" src/renderer/src/views/SustentacaoView.tsx` — deve retornar 1 (dentro de MiniLineChart)
2. `grep -n "SLA Compliance" src/renderer/src/views/SustentacaoView.tsx` — deve retornar 2 linhas (7d e 30d)
3. `grep -n "null ? \`\${" src/renderer/src/views/SustentacaoView.tsx` — deve existir verificação de null para compliance
4. O app compila sem erros TypeScript (`npx tsc --noEmit`)
5. Verificação visual: abrir SustentacaoView com dados carregados e confirmar que os novos cards aparecem
</verification>

<success_criteria>
- SustentacaoView exibe 2 novos cards no topo: SLA Compliance 7d e SLA Compliance 30d
- Cards existentes têm indicadores de delta (↑/↓/=) vs 7 dias atrás, ou '—' sem histórico
- Mini charts SVG aparecem quando history.length >= 2, ocultos quando sem histórico
- Sem crashes quando history[] está vazio (primeiro uso)
- Todos os novos elementos seguem o padrão visual inline style do projeto
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/999.11-sustentacao-trending-sla-compliance/999.11-03-SUMMARY.md`
</output>
