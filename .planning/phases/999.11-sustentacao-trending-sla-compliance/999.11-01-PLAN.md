---
phase: 999.11-sustentacao-trending-sla-compliance
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/types/ipc.ts
  - src/renderer/src/types/global.d.ts
  - src/main/external/SupportBoardClient.ts
autonomous: true
requirements:
  - STRD-01
  - STRD-02

must_haves:
  truths:
    - "SupportBoardSnapshot tem campos complianceRate7d e complianceRate30d (ambos number | null)"
    - "Existe tipo SustentacaoHistoryEntry com campos date, fetchedAt, ticketsAbertos, ticketsFechadosUltimos30d, breachCount, complianceRate7d, complianceRate30d"
    - "SupportBoardSnapshot tem campo history: SustentacaoHistoryEntry[]"
    - "fetchSupportBoardMetrics() calcula compliance7d e compliance30d antes de retornar — usando JiraIssue[] completo em memória"
    - "global.d.ts declara window.api.sustentacao com getData, refresh e runAnalysis tipados"
  artifacts:
    - path: "src/renderer/src/types/ipc.ts"
      provides: "SustentacaoHistoryEntry type + extended SupportBoardSnapshot"
      contains: "SustentacaoHistoryEntry"
    - path: "src/renderer/src/types/global.d.ts"
      provides: "window.api.sustentacao type declaration"
      contains: "sustentacao"
    - path: "src/main/external/SupportBoardClient.ts"
      provides: "calcularCompliance function + compliance fields in return"
      contains: "calcularCompliance"
  key_links:
    - from: "src/main/external/SupportBoardClient.ts"
      to: "src/renderer/src/types/ipc.ts"
      via: "import SupportBoardSnapshot — must match extended type"
      pattern: "complianceRate7d"
---

<objective>
Definir os contratos de tipos para trending + compliance e implementar o cálculo de compliance no SupportBoardClient.

Purpose: Plans subsequentes dependem dos tipos corretos. O compliance deve ser calculado onde os dados de JiraIssue[] estão disponíveis (Main Process), não no renderer.
Output: Tipos estendidos em ipc.ts, global.d.ts corrigido, compliance calculado e retornado por fetchSupportBoardMetrics().
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/999.11-sustentacao-trending-sla-compliance/999.11-CONTEXT.md
@.planning/phases/999.11-sustentacao-trending-sla-compliance/999.11-RESEARCH.md
</context>

<interfaces>
<!-- Tipos existentes que serão estendidos. Ler antes de modificar. -->

De src/renderer/src/types/ipc.ts (linhas 155-184):
```typescript
export interface SupportTicket {
  key: string
  summary: string
  type: string
  labels: string[]
  assignee: string | null
  ageDias: number
  status: string
  slaBreached: boolean
  recentComments: Array<{ author: string; body: string; created: string }>
}

export interface SupportBoardSnapshot {
  atualizadoEm: string
  ticketsAbertos: number
  ticketsFechadosUltimos30d: number
  topTipos: Array<{ tipo: string; count: number }>
  topLabels: Array<{ label: string; count: number }>
  ticketsEmBreach: SupportTicket[]
  porAssignee: Record<string, number>
}
```

De src/main/external/SupportBoardClient.ts — fetchSupportBoardMetrics retorna SupportBoardSnapshot.
JiraIssue tem campos: key, summary, type, labels, assignee, status, statusCategory, created, resolved (string | null).
Função ageDias(createdAt: string): number já existe no arquivo (linha 18).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Estender tipos em ipc.ts e corrigir global.d.ts</name>
  <files>src/renderer/src/types/ipc.ts, src/renderer/src/types/global.d.ts</files>

  <read_first>
    - src/renderer/src/types/ipc.ts — ler arquivo completo para ver estrutura atual e onde inserir novos tipos
    - src/renderer/src/types/global.d.ts — ler arquivo completo para ver onde adicionar bloco sustentacao
    - src/preload/index.ts (linhas 137-141) — ver assinatura exata dos métodos sustentacao expostos no preload
  </read_first>

  <action>
**1. Em src/renderer/src/types/ipc.ts:**

Após a definição de `SupportBoardSnapshot` (linha ~184), adicionar o novo tipo:

```typescript
/** Entrada de histórico diário de sustentação (sem ticketsEmBreach completo para manter history.json leve) */
export interface SustentacaoHistoryEntry {
  /** Data no formato YYYY-MM-DD (chave de deduplica diária) */
  date: string
  /** Timestamp Unix ms do fetch */
  fetchedAt: number
  ticketsAbertos: number
  ticketsFechadosUltimos30d: number
  /** Apenas o número, não o array completo de SupportTicket */
  breachCount: number
  /** null = sem tickets resolvidos na janela (não exibir percentual, exibir "—") */
  complianceRate7d: number | null
  /** null = sem tickets resolvidos na janela */
  complianceRate30d: number | null
}
```

Estender `SupportBoardSnapshot` com os seguintes campos adicionais (append dentro da interface existente, após `porAssignee`):

```typescript
  /** null = sem tickets resolvidos nos últimos 7 dias */
  complianceRate7d: number | null
  /** null = sem tickets resolvidos nos últimos 30 dias */
  complianceRate30d: number | null
  /** Últimos 30 snapshots diários para deltas e mini charts */
  history: SustentacaoHistoryEntry[]
```

**2. Em src/renderer/src/types/global.d.ts:**

Localizar a linha com `import type { ... }` no topo e adicionar `SupportBoardSnapshot` à lista de imports se não estiver presente.

Dentro do bloco `interface Window { api: { ... } }`, após o bloco `external: { ... }` (ou antes de `shell:`), adicionar o bloco sustentacao:

```typescript
      sustentacao: {
        getData:     () => Promise<SupportBoardSnapshot | null>
        refresh:     () => Promise<SupportBoardSnapshot | null>
        runAnalysis: () => Promise<{ analysis?: string; error?: string } | null>
      }
```

Nota: `SupportBoardSnapshot` aqui é o tipo estendido com os novos campos — importar de `./ipc`.
  </action>

  <verify>
    <automated>cd /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit && grep -n "SustentacaoHistoryEntry" src/renderer/src/types/ipc.ts && grep -n "complianceRate7d" src/renderer/src/types/ipc.ts && grep -n "history: SustentacaoHistoryEntry" src/renderer/src/types/ipc.ts && grep -n "sustentacao" src/renderer/src/types/global.d.ts</automated>
  </verify>

  <acceptance_criteria>
    - ipc.ts contém `export interface SustentacaoHistoryEntry {` com campos: date, fetchedAt, ticketsAbertos, ticketsFechadosUltimos30d, breachCount, complianceRate7d, complianceRate30d
    - ipc.ts contém `complianceRate7d: number | null` dentro de SupportBoardSnapshot
    - ipc.ts contém `history: SustentacaoHistoryEntry[]` dentro de SupportBoardSnapshot
    - global.d.ts contém bloco `sustentacao:` com `getData`, `refresh`, `runAnalysis`
    - `npx tsc --noEmit` passa sem erros nos arquivos modificados (ou erros pré-existentes não relacionados)
  </acceptance_criteria>

  <done>Contratos de tipos definidos. ipc.ts tem SustentacaoHistoryEntry + SupportBoardSnapshot estendido. global.d.ts tem sustentacao tipado.</done>
</task>

<task type="auto">
  <name>Task 2: Implementar calcularCompliance() em SupportBoardClient.ts</name>
  <files>src/main/external/SupportBoardClient.ts</files>

  <read_first>
    - src/main/external/SupportBoardClient.ts — ler arquivo completo para ver estrutura, função ageDias, tipo JiraIssue, e o return do fetchSupportBoardMetrics
    - src/main/external/JiraClient.ts — verificar campos do tipo JiraIssue, especialmente `resolved`, `statusCategory`, `created`, `type`
  </read_first>

  <action>
**1. Adicionar função `calcularCompliance` antes de `fetchSupportBoardMetrics`:**

```typescript
/**
 * Calcula SLA compliance rate para uma janela de dias.
 * Retorna null quando não há tickets resolvidos na janela (exibir "—" na UI, não 100%).
 * Ticket fechado no mesmo dia que aberto (ageDias = 0) conta como compliant (0 <= threshold).
 */
function calcularCompliance(
  issues: Array<{ statusCategory: string; resolved: string | null; created: string; type: string }>,
  slaThresholds: Record<string, number>,
  windowDias: number
): number | null {
  const cutoff = Date.now() - windowDias * 24 * 60 * 60 * 1000
  const resolved = issues.filter(
    (i) =>
      i.statusCategory === 'done' &&
      i.resolved !== null &&
      new Date(i.resolved).getTime() >= cutoff
  )

  if (resolved.length === 0) return null // sem dados = não exibir percentual

  const compliant = resolved.filter((i) => {
    const threshold = slaThresholds[i.type] ?? DEFAULT_SLA_DIAS
    const ageMs = new Date(i.resolved!).getTime() - new Date(i.created).getTime()
    const ageDiasResolved = Math.floor(ageMs / (1000 * 60 * 60 * 24))
    return ageDiasResolved <= threshold
  })

  return Math.round((compliant.length / resolved.length) * 100)
}
```

**2. Em `fetchSupportBoardMetrics`, após construir `ticketsEmBreach` e antes do `return`, calcular compliance:**

```typescript
  // Calcular compliance rates (usa JiraIssue[] completo — não disponível no snapshot)
  const complianceRate7d = calcularCompliance(issues, slaThresholds, 7)
  const complianceRate30d = calcularCompliance(issues, slaThresholds, 30)
```

**3. No `return` de `fetchSupportBoardMetrics`, adicionar os novos campos:**

```typescript
  return {
    atualizadoEm: new Date().toISOString(),
    ticketsAbertos: abertos.length,
    ticketsFechadosUltimos30d: fechados30d.length,
    topTipos,
    topLabels,
    ticketsEmBreach,
    porAssignee: assigneeCounts,
    complianceRate7d,
    complianceRate30d,
    history: [], // preenchido pelo IPC handler após ler history.json (ver Plan 02)
  }
```

**Nota sobre o campo `history: []`:** O SupportBoardClient não tem acesso ao arquivo history.json (ele não conhece workspacePath). O array vazio aqui é correto — o IPC handler em index.ts (Plan 02) irá substituir esse campo com os dados lidos do disco ao montar a resposta final. Esta separação de responsabilidades está alinhada com o padrão do projeto.
  </action>

  <verify>
    <automated>cd /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit && grep -n "calcularCompliance" src/main/external/SupportBoardClient.ts && grep -n "complianceRate7d" src/main/external/SupportBoardClient.ts && grep -n "history: \[\]" src/main/external/SupportBoardClient.ts</automated>
  </verify>

  <acceptance_criteria>
    - SupportBoardClient.ts contém função `calcularCompliance(` com parâmetros `issues`, `slaThresholds`, `windowDias`
    - SupportBoardClient.ts contém `complianceRate7d: calcularCompliance(issues, slaThresholds, 7)`
    - SupportBoardClient.ts contém `complianceRate30d: calcularCompliance(issues, slaThresholds, 30)`
    - SupportBoardClient.ts contém `history: []` no return de fetchSupportBoardMetrics
    - A função retorna `null` quando `resolved.length === 0` (grep: `if (resolved.length === 0) return null`)
  </acceptance_criteria>

  <done>calcularCompliance() implementada. fetchSupportBoardMetrics() retorna complianceRate7d, complianceRate30d e history (vazio — preenchido pelo IPC).</done>
</task>

</tasks>

<verification>
Após completar as tasks:
1. `grep -n "SustentacaoHistoryEntry" src/renderer/src/types/ipc.ts` — deve retornar a definição da interface
2. `grep -n "history: SustentacaoHistoryEntry\[\]" src/renderer/src/types/ipc.ts` — deve existir dentro de SupportBoardSnapshot
3. `grep -n "sustentacao:" src/renderer/src/types/global.d.ts` — deve existir o bloco tipado
4. `grep -n "calcularCompliance" src/main/external/SupportBoardClient.ts` — deve existir a função e 2 chamadas
5. TypeScript não reporta erros novos nos arquivos modificados
</verification>

<success_criteria>
- SupportBoardSnapshot tem complianceRate7d, complianceRate30d e history nos tipos
- SustentacaoHistoryEntry definido e exportado em ipc.ts
- global.d.ts declara window.api.sustentacao com tipos corretos
- fetchSupportBoardMetrics() calcula e retorna compliance rates
- Sem erros de TypeScript introduzidos por esta fase
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/999.11-sustentacao-trending-sla-compliance/999.11-01-SUMMARY.md`
</output>
