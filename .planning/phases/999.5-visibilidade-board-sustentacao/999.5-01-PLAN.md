---
phase: 999.5-visibilidade-board-sustentacao
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/types/ipc.ts
  - src/main/registry/SettingsManager.ts
  - src/renderer/src/router.tsx
autonomous: true
requirements:
  - SUST-01
  - SUST-02

must_haves:
  truths:
    - "AppSettings aceita jiraSupportBoardId, jiraSupportProjectKey e jiraSlaThresholds sem quebrar settings existentes"
    - "Tipos SupportTicket e SupportBoardSnapshot estão exportados de ipc.ts e usáveis no main e renderer"
    - "ViewName inclui 'sustentacao' — navigate('sustentacao') compila sem erro TypeScript"
  artifacts:
    - path: "src/renderer/src/types/ipc.ts"
      provides: "SupportTicket, SupportBoardSnapshot, novos campos em AppSettings"
      contains: "SupportBoardSnapshot"
    - path: "src/main/registry/SettingsManager.ts"
      provides: "AppSettings com campos de sustentação"
      contains: "jiraSupportBoardId"
    - path: "src/renderer/src/router.tsx"
      provides: "ViewName com 'sustentacao'"
      contains: "'sustentacao'"
  key_links:
    - from: "src/renderer/src/types/ipc.ts"
      to: "src/main/registry/SettingsManager.ts"
      via: "AppSettings type espelhada em ambos"
      pattern: "jiraSupportBoardId"
---

<objective>
Definir os contratos de tipos que toda a fase 999.5 depende: novos tipos IPC para dados do board de sustentação, novos campos em AppSettings (em ambos os arquivos que espelham a interface), e adição de 'sustentacao' ao ViewName do router.

Purpose: Sem esses contratos, nenhum plano posterior pode ser compilado. Esta é a fundação de tipos que elimina scavenger hunts nos planos de implementação.
Output: ipc.ts com SupportTicket + SupportBoardSnapshot + campos AppSettings; SettingsManager.ts com novos campos; router.tsx com 'sustentacao' em ViewName.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/999.5-visibilidade-board-sustentacao/999.5-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar SupportTicket e SupportBoardSnapshot em ipc.ts e novos campos AppSettings</name>
  <files>src/renderer/src/types/ipc.ts</files>
  <read_first>
    - src/renderer/src/types/ipc.ts (estado atual completo — ler antes de qualquer edição)
  </read_first>
  <action>
Adicionar no arquivo `src/renderer/src/types/ipc.ts`:

1. Novos campos em `AppSettings` (após o bloco `// Relatórios`, antes do fechamento da interface):

```typescript
  // Sustentação
  jiraSupportBoardId?: number
  jiraSupportProjectKey?: string
  /** Threshold de SLA por tipo de issue (tipo → dias). Padrão: todos os tipos com 5 dias. Ex: { Bug: 3, Task: 7 } */
  jiraSlaThresholds?: Record<string, number>
```

2. Novas interfaces (adicionar após `ExternalDataSnapshot` e antes de `ExternalHistoricoEntry`):

```typescript
export interface SupportTicket {
  key: string
  summary: string
  type: string
  labels: string[]
  assignee: string | null
  /** Dias desde criação */
  ageDias: number
  status: string
  /** true se age > threshold configurado para seu tipo */
  slaBreached: boolean
  /** Últimos 3 comentários (body + author) */
  recentComments: Array<{ author: string; body: string; created: string }>
}

export interface SupportBoardSnapshot {
  atualizadoEm: string
  /** Tickets abertos (status != Done) nos últimos 30 dias ou abertos antes disso */
  ticketsAbertos: number
  /** Tickets fechados nos últimos 30 dias */
  ticketsFechadosUltimos30d: number
  /** Top 5 tipos por frequência */
  topTipos: Array<{ tipo: string; count: number }>
  /** Top 5 labels por frequência */
  topLabels: Array<{ label: string; count: number }>
  /** Tickets com SLA estourado */
  ticketsEmBreach: SupportTicket[]
  /** Agrupamento por assignee: slug → contagem de tickets abertos */
  porAssignee: Record<string, number>
}
```
  </action>
  <verify>
    <automated>grep -n "SupportBoardSnapshot\|jiraSupportBoardId\|jiraSlaThresholds" /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit/src/renderer/src/types/ipc.ts</automated>
  </verify>
  <acceptance_criteria>
    - ipc.ts contém `export interface SupportTicket {`
    - ipc.ts contém `export interface SupportBoardSnapshot {`
    - ipc.ts contém `jiraSupportBoardId?: number`
    - ipc.ts contém `jiraSupportProjectKey?: string`
    - ipc.ts contém `jiraSlaThresholds?: Record<string, number>`
    - ipc.ts contém `recentComments: Array<`
    - ipc.ts contém `porAssignee: Record<string, number>`
  </acceptance_criteria>
  <done>Tipos de sustentação exportados e AppSettings estendido em ipc.ts</done>
</task>

<task type="auto">
  <name>Task 2: Espelhar novos campos AppSettings em SettingsManager.ts e adicionar 'sustentacao' ao ViewName</name>
  <files>src/main/registry/SettingsManager.ts, src/renderer/src/router.tsx</files>
  <read_first>
    - src/main/registry/SettingsManager.ts (ler inteiro — especialmente interface AppSettings e const DEFAULTS)
    - src/renderer/src/router.tsx (ler inteiro — especialmente type ViewName)
  </read_first>
  <action>
**SettingsManager.ts:** Adicionar os mesmos campos na interface `AppSettings` local (após o bloco `// Relatórios`):

```typescript
  // Sustentação
  jiraSupportBoardId?: number
  jiraSupportProjectKey?: string
  /** Threshold de SLA por tipo de issue (tipo → dias). Padrão: todos os tipos com 5 dias. */
  jiraSlaThresholds?: Record<string, number>
```

Não alterar `const DEFAULTS` — campos opcionais não precisam de valor padrão (settings existentes permanecem válidos).

**router.tsx:** Adicionar `'sustentacao'` ao union type `ViewName`:

```typescript
export type ViewName =
  | 'dashboard'
  | 'pares'
  | 'gestores'
  | 'feed'
  | 'person'
  | 'person-form'
  | 'inbox'
  | 'settings'
  | 'eu'
  | 'refinamentos'
  | 'logs'
  | 'reports'
  | 'audit'
  | 'sustentacao'
```
  </action>
  <verify>
    <automated>grep -n "jiraSupportBoardId\|jiraSlaThresholds" /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit/src/main/registry/SettingsManager.ts && grep -n "sustentacao" /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit/src/renderer/src/router.tsx</automated>
  </verify>
  <acceptance_criteria>
    - SettingsManager.ts contém `jiraSupportBoardId?: number`
    - SettingsManager.ts contém `jiraSupportProjectKey?: string`
    - SettingsManager.ts contém `jiraSlaThresholds?: Record<string, number>`
    - router.tsx contém `| 'sustentacao'`
    - A posição de 'sustentacao' no union type está após 'audit' (não no meio de tipos existentes)
  </acceptance_criteria>
  <done>SettingsManager.ts e router.tsx atualizados — base de tipos pronta para os planos de implementação</done>
</task>

</tasks>

<verification>
Após execução:
- `grep -rn "SupportBoardSnapshot" src/renderer/src/types/ipc.ts` retorna matches
- `grep -rn "jiraSupportBoardId" src/main/registry/SettingsManager.ts src/renderer/src/types/ipc.ts` retorna matches em ambos os arquivos
- `grep -n "sustentacao" src/renderer/src/router.tsx` retorna `| 'sustentacao'`
- `npx tsc --noEmit` não introduz novos erros de tipo (campos opcionais são backward-compatible)
</verification>

<success_criteria>
- SupportTicket e SupportBoardSnapshot exportados de ipc.ts com todos os campos especificados
- AppSettings com jiraSupportBoardId, jiraSupportProjectKey e jiraSlaThresholds em ambos os arquivos (ipc.ts e SettingsManager.ts)
- ViewName inclui 'sustentacao' em router.tsx
- Zero regressão em settings existentes (campos opcionais, sem alterar DEFAULTS)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/999.5-visibilidade-board-sustentacao/999.5-01-SUMMARY.md`
</output>
