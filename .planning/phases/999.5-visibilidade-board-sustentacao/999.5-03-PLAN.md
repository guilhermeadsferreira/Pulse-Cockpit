---
phase: 999.5-visibilidade-board-sustentacao
plan: 03
type: execute
wave: 2
depends_on:
  - 999.5-01
files_modified:
  - src/renderer/src/views/SettingsView.tsx
autonomous: true
requirements:
  - SUST-02

must_haves:
  truths:
    - "SettingsView exibe campos para jiraSupportBoardId, jiraSupportProjectKey e jiraSlaThresholds"
    - "Campos de sustentação ficam numa seção dedicada 'Board de Sustentação' dentro das configurações de Jira"
    - "Valores são salvos via settings.save() junto com o restante das configurações"
  artifacts:
    - path: "src/renderer/src/views/SettingsView.tsx"
      provides: "UI para configurar o board de sustentação"
      contains: "jiraSupportBoardId"
  key_links:
    - from: "src/renderer/src/views/SettingsView.tsx"
      to: "src/renderer/src/types/ipc.ts"
      via: "AppSettings type com campos de sustentação"
      pattern: "jiraSupportBoardId"
---

<objective>
Adicionar os campos de configuração do board de sustentação na SettingsView existente: jiraSupportBoardId (número), jiraSupportProjectKey (texto) e jiraSlaThresholds (JSON inline editável). Esses campos ficam numa subseção dedicada dentro do grupo de configurações Jira já existente.

Purpose: Sem UI de configuração, o gestor não consegue conectar o board de sustentação. Esta é a única interação obrigatória do usuário antes de usar o módulo.
Output: SettingsView.tsx atualizada com seção "Board de Sustentação" funcional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.5-visibilidade-board-sustentacao/999.5-CONTEXT.md
@.planning/phases/999.5-visibilidade-board-sustentacao/999.5-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar campos do board de sustentação na SettingsView</name>
  <files>src/renderer/src/views/SettingsView.tsx</files>
  <read_first>
    - src/renderer/src/views/SettingsView.tsx (ler inteiro — especialmente a seção de Jira e como os campos de número/texto são renderizados)
  </read_first>
  <action>
Localizar a seção de configurações Jira na SettingsView (contém `jiraBaseUrl`, `jiraEmail`, `jiraApiToken`, `jiraBoardId`). Após o último campo Jira existente, adicionar uma subseção "Board de Sustentação" com os seguintes campos:

**1. Campo jiraSupportProjectKey (texto):**
- Label: "Project Key do Board de Sustentação"
- Placeholder: "ex: SUP, HELP, ATC"
- `value={settings.jiraSupportProjectKey ?? ''}`
- `onChange` atualiza `settings.jiraSupportProjectKey`
- Seguir o mesmo padrão visual de `jiraProjectKey` já existente

**2. Campo jiraSupportBoardId (número):**
- Label: "Board ID do Board de Sustentação"
- Placeholder: "ex: 42"
- `value={settings.jiraSupportBoardId ?? ''}`
- `onChange` converte para número: `Number(e.target.value) || undefined`
- Seguir o mesmo padrão visual de `jiraBoardId` já existente

**3. Campo jiraSlaThresholds (textarea JSON):**
- Label: "SLA Thresholds por Tipo (JSON)"
- Placeholder: `'{"Bug": 3, "Task": 7, "Story": 5}'`
- Usar estado local `slaThresholdsText` (string) para edição
- Inicializar `slaThresholdsText` via `useEffect` quando settings carregam: `JSON.stringify(settings.jiraSlaThresholds ?? {}, null, 2)`
- No `onBlur` (não onChange) tentar `JSON.parse(slaThresholdsText)` — se válido, atualiza `settings.jiraSlaThresholds`; se inválido, mostrar erro inline "JSON inválido"
- Textarea com `rows={3}`, font-family monospace
- Seguir padrão de estilo dos outros campos (mesmo border, padding, background)

**Separador visual:** Adicionar um `<div>` com borderTop, marginTop e marginBottom para delimitar a subseção "Board de Sustentação" dos campos Jira principais. Usar heading `<div style={{ fontWeight: 500, marginBottom: 8 }}>Board de Sustentação</div>` como título da subseção.

Não criar novo componente — adicionar inline no JSX da seção Jira, seguindo o padrão existente do arquivo.
  </action>
  <verify>
    <automated>grep -n "jiraSupportProjectKey\|jiraSupportBoardId\|jiraSlaThresholds\|Board de Sustentação" /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit/src/renderer/src/views/SettingsView.tsx</automated>
  </verify>
  <acceptance_criteria>
    - SettingsView.tsx contém `jiraSupportProjectKey`
    - SettingsView.tsx contém `jiraSupportBoardId`
    - SettingsView.tsx contém `jiraSlaThresholds`
    - SettingsView.tsx contém "Board de Sustentação" como texto de label/heading
    - SettingsView.tsx contém `slaThresholdsText` como estado local (useState)
    - SettingsView.tsx contém `onBlur` para o campo de thresholds (parse JSON)
    - SettingsView.tsx contém `JSON.parse(` para validação do JSON
    - Os novos campos ficam APÓS os campos Jira existentes (jiraBaseUrl, jiraEmail, jiraApiToken, jiraBoardId) — não antes
  </acceptance_criteria>
  <done>SettingsView.tsx atualizada com seção de configuração do board de sustentação funcional</done>
</task>

</tasks>

<verification>
- `grep -c "jiraSupportProjectKey\|jiraSupportBoardId\|jiraSlaThresholds" src/renderer/src/views/SettingsView.tsx` retorna >= 3 linhas
- `grep -n "Board de Sustentação" src/renderer/src/views/SettingsView.tsx` confirma seção dedicada
</verification>

<success_criteria>
- Campos jiraSupportProjectKey, jiraSupportBoardId e jiraSlaThresholds visíveis e funcionais na SettingsView
- jiraSlaThresholds usa textarea JSON com validação no onBlur
- Seção delimitada visualmente (separador + título "Board de Sustentação")
- Valores salvos via o mecanismo de save existente da SettingsView
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/999.5-visibilidade-board-sustentacao/999.5-03-SUMMARY.md`
</output>
