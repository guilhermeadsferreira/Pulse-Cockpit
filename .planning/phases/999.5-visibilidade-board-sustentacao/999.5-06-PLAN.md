---
phase: 999.5-visibilidade-board-sustentacao
plan: 06
type: execute
wave: 4
depends_on:
  - 999.5-04
  - 999.5-05
files_modified:
  - src/main/prompts/sustentacao-analysis.prompt.ts
  - src/main/external/MetricsWriter.ts
  - src/main/index.ts
autonomous: true
requirements:
  - SUST-03
  - SUST-05

must_haves:
  truths:
    - "Botão 'Analisar' na SustentacaoView dispara análise de IA e retorna resultado"
    - "Prompt extrai padrões recorrentes, oportunidades de automação, causa raiz e sugestões de redução de SLA"
    - "Resultado persiste no metricas.md do top assignee como bloco SUSTENTACAO:ANALISE via MetricsWriter"
    - "Se há múltiplos assignees com tickets significativos (>= 3), cada um recebe insights dos seus tickets"
    - "Falha na persistência não impede retorno do resultado ao frontend (graceful)"
  artifacts:
    - path: "src/main/prompts/sustentacao-analysis.prompt.ts"
      provides: "buildSustentacaoPrompt(tickets) → string"
      exports: ["buildSustentacaoPrompt"]
    - path: "src/main/external/MetricsWriter.ts"
      provides: "writeSustentacaoAnalysis(slug, analysis) → void"
      contains: "SUSTENTACAO:ANALISE"
    - path: "src/main/index.ts"
      provides: "IPC handler sustentacao:run-analysis"
      contains: "sustentacao:run-analysis"
  key_links:
    - from: "src/main/index.ts"
      to: "src/main/prompts/sustentacao-analysis.prompt.ts"
      via: "buildSustentacaoPrompt chamado no handler de análise"
      pattern: "buildSustentacaoPrompt"
    - from: "src/main/index.ts"
      to: "src/main/external/MetricsWriter.ts"
      via: "metricsWriter.writeSustentacaoAnalysis(slug, analysis)"
      pattern: "writeSustentacaoAnalysis"
---

<objective>
Completar o módulo de sustentação com análise de IA: criar o prompt especializado, adicionar writeSustentacaoAnalysis ao MetricsWriter (bloco SUSTENTACAO:ANALISE em metricas.md), e registrar o IPC handler sustentacao:run-analysis que orquestra tudo.

Purpose: A análise de IA é o valor principal do módulo — sem ela, o gestor vê apenas métricas brutas. O prompt extrai oportunidades de automação e causa raiz dos tickets de suporte.
Output: Prompt, MetricsWriter estendido, IPC handler de análise.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.5-visibilidade-board-sustentacao/999.5-CONTEXT.md
@.planning/phases/999.5-visibilidade-board-sustentacao/999.5-04-SUMMARY.md
@.planning/phases/999.5-visibilidade-board-sustentacao/999.5-05-SUMMARY.md
</context>

<interfaces>
<!-- Contratos que este plano usa e cria -->

De src/main/external/MetricsWriter.ts (padrão de blocos gerenciados — Phase 999.7):
```typescript
// SECTIONS usa comentários HTML: <!-- METRICAS:SECTION_NAME --> e <!-- FIM:SECTION_NAME -->
// Padrão de escrita de seção: replaceSection(content, sectionKey, newContent)
// Padrão de criação de arquivo: ensureFile(slug) → path do metricas.md

// Exemplo de writeAlerts (para referência do padrão):
writeAlerts(slug: string, alerts: AlertEntry[]): void
// Que usa internamente: this.replaceSection(content, 'ALERTAS', rendered)
```

De src/main/external/SupportBoardClient.ts (Plan 02):
```typescript
export interface SupportBoardSnapshot {
  ticketsEmBreach: SupportTicket[]
  porAssignee: Record<string, number>
  topTipos: Array<{ tipo: string; count: number }>
  // ...
}

export interface SupportTicket {
  key: string
  summary: string
  type: string
  assignee: string | null
  ageDias: number
  recentComments: Array<{ author: string; body: string; created: string }>
}
```

ClaudeRunner (padrão de invocação de IA — verificar como cycleReport ou agenda chamam):
```typescript
// Padrão: ClaudeRunner.run(prompt, { model: 'haiku' }) → string
// Importação típica: import { ClaudeRunner } from './ClaudeRunner'
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Criar prompt de análise de sustentação e adicionar writeSustentacaoAnalysis ao MetricsWriter</name>
  <files>src/main/prompts/sustentacao-analysis.prompt.ts, src/main/external/MetricsWriter.ts</files>
  <read_first>
    - src/main/external/MetricsWriter.ts (ler inteiro — SECTIONS, replaceSection, ensureFile, writeAlerts como referência)
    - src/main/prompts/ (listar e ler um dos prompts existentes para ver padrão de buildXxxPrompt)
  </read_first>
  <action>
**Criar src/main/prompts/sustentacao-analysis.prompt.ts:**

```typescript
import type { SupportBoardSnapshot } from '../../renderer/src/types/ipc'

export function buildSustentacaoPrompt(snapshot: SupportBoardSnapshot): string {
  const ticketsStr = snapshot.ticketsEmBreach.map((t) => {
    const comments = t.recentComments.length > 0
      ? t.recentComments.map((c) => `    - [${c.author}]: ${c.body.slice(0, 200)}`).join('\n')
      : '    (sem comentários)'

    return `- ${t.key}: ${t.summary}
  Tipo: ${t.type} | Assignee: ${t.assignee ?? 'sem assignee'} | Idade: ${t.ageDias}d
  Comentários recentes:
${comments}`
  }).join('\n\n')

  const tiposStr = snapshot.topTipos.map((t) => `${t.tipo}: ${t.count}`).join(', ')

  return `Você é um analista de engenharia de software. Analise os tickets de sustentação abaixo e gere insights acionáveis para o gestor.

## Contexto do Board de Sustentação

- Tickets abertos: ${snapshot.ticketsAbertos}
- Fechados nos últimos 30 dias: ${snapshot.ticketsFechadosUltimos30d}
- Distribuição por tipo: ${tiposStr}
- Tickets em breach de SLA (${snapshot.ticketsEmBreach.length} total):

${ticketsStr || '(nenhum ticket em breach)'}

## Instruções

Gere uma análise estruturada com as seguintes seções:

### Padrões Recorrentes
Liste os 2-4 tipos de problema mais frequentes nos tickets. Para cada um: tipo de problema + frequência + impacto.

### Oportunidades de Automação
Liste 2-3 categorias de tickets que poderiam ser resolvidas sem intervenção humana, com sugestão concreta de automação (ex: "tickets de reset de senha → self-service portal").

### Causa Raiz Provável
Para os tipos de problema mais frequentes, identifique a causa raiz técnica ou de processo mais provável com base nos títulos e comentários.

### Sugestões de Redução de SLA
Liste 2-3 mudanças concretas de processo ou triagem que poderiam reduzir o tempo de resolução.

Seja direto e específico. Evite generalidades. Baseie-se apenas nos dados fornecidos.`
}
```

**Adicionar em src/main/external/MetricsWriter.ts:**

1. Adicionar nova seção em `SECTIONS`:
```typescript
SUSTENTACAO_ANALISE: {
  open: '<!-- METRICAS:SUSTENTACAO_ANALISE -->',
  close: '<!-- FIM:SUSTENTACAO_ANALISE -->'
},
```

2. Adicionar método público `writeSustentacaoAnalysis`:
```typescript
writeSustentacaoAnalysis(slug: string, analysis: string): void {
  const filePath = this.ensureFile(slug)
  const content = readFileSync(filePath, 'utf-8')
  const date = new Date().toISOString().slice(0, 10)
  const rendered = `**Análise de Sustentação — ${date}**\n\n${analysis}`
  const updated = this.replaceSection(content, 'SUSTENTACAO_ANALISE' as SectionKey, rendered)
  writeFileSync(filePath, updated, 'utf-8')
  log.info('MetricsWriter: sustentação análise salva', { slug })
}
```

**Nota:** Verificar a assinatura de `replaceSection` no MetricsWriter existente e chamar exatamente como os outros métodos públicos (writeAlerts, writeWeekly etc.). Garantir que `'SUSTENTACAO_ANALISE'` é compatível com o tipo `SectionKey`.
  </action>
  <verify>
    <automated>test -f /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit/src/main/prompts/sustentacao-analysis.prompt.ts && grep -n "buildSustentacaoPrompt\|writeSustentacaoAnalysis\|SUSTENTACAO_ANALISE" /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit/src/main/external/MetricsWriter.ts</automated>
  </verify>
  <acceptance_criteria>
    - sustentacao-analysis.prompt.ts existe em src/main/prompts/
    - sustentacao-analysis.prompt.ts exporta `buildSustentacaoPrompt`
    - sustentacao-analysis.prompt.ts contém as seções: "Padrões Recorrentes", "Oportunidades de Automação", "Causa Raiz Provável", "Sugestões de Redução de SLA"
    - MetricsWriter.ts contém `SUSTENTACAO_ANALISE` em SECTIONS
    - MetricsWriter.ts contém `writeSustentacaoAnalysis(slug: string, analysis: string): void`
    - MetricsWriter.ts contém `<!-- METRICAS:SUSTENTACAO_ANALISE -->`
  </acceptance_criteria>
  <done>Prompt de análise criado e MetricsWriter estendido com bloco SUSTENTACAO:ANALISE</done>
</task>

<task type="auto">
  <name>Task 2: Registrar IPC handler sustentacao:run-analysis em index.ts</name>
  <files>src/main/index.ts</files>
  <read_first>
    - src/main/index.ts (ver como ai:generate-agenda ou ai:cycle-report chamam ClaudeRunner — padrão de invocação de IA)
    - src/main/external/MetricsWriter.ts (writeSustentacaoAnalysis — assinatura final após Task 1)
  </read_first>
  <action>
Adicionar import no topo de index.ts:
```typescript
import { buildSustentacaoPrompt } from './prompts/sustentacao-analysis.prompt'
```

Adicionar handler `sustentacao:run-analysis` após os handlers `sustentacao:get-data` e `sustentacao:refresh` (adicionados no Plan 04):

```typescript
ipcMain.handle('sustentacao:run-analysis', async () => {
  const settings = SettingsManager.load()
  const { jiraSupportProjectKey, workspacePath } = settings

  if (!jiraSupportProjectKey) {
    return { error: 'Board de sustentação não configurado' }
  }

  // Buscar snapshot atual (usa cache se disponível)
  const snapshot = await ipcMain.emit('sustentacao:get-data', null)
    .catch(() => null) as SupportBoardSnapshot | null

  // Se não há snapshot, tentar buscar diretamente
  // [Nota ao executor: verificar se é possível reusar a função de fetch criada no Plan 04
  //  ou se é necessário chamar fetchSupportBoardMetrics novamente aqui]

  if (!snapshot) {
    return { error: 'Dados do board indisponíveis — atualize antes de analisar' }
  }

  const prompt = buildSustentacaoPrompt(snapshot)

  log.info('sustentacao:run-analysis iniciando análise', {
    ticketsEmBreach: snapshot.ticketsEmBreach.length,
    promptLength: prompt.length,
  })

  // Invocar Claude CLI via ClaudeRunner — padrão do projeto
  const analysis = await runClaude(prompt, { model: settings.claudeDefaultModel ?? 'haiku' })

  log.info('sustentacao:run-analysis análise concluída', { analysisLength: analysis.length })

  // Persistir no metricas.md de cada assignee com >= 3 tickets
  const SIGNIFICANT_TICKET_THRESHOLD = 3
  const metricsWriter = new MetricsWriter(workspacePath)
  const people = PersonRegistry.list(workspacePath)

  for (const [assigneeKey, count] of Object.entries(snapshot.porAssignee)) {
    if (count < SIGNIFICANT_TICKET_THRESHOLD) continue

    // Mapear assigneeKey (email ou nome do Jira) para slug da pessoa
    // Estratégia: buscar pessoa onde config.jiraEmail === assigneeKey
    // ou nome normalizado contém assigneeKey
    const person = people.find(
      (p) => p.jiraEmail === assigneeKey ||
             p.nome.toLowerCase().includes(assigneeKey.toLowerCase()) ||
             assigneeKey.toLowerCase().includes(p.nome.split(' ')[0].toLowerCase())
    )

    if (!person) {
      log.warn('sustentacao:run-analysis assignee sem match no PersonRegistry', { assigneeKey })
      continue
    }

    try {
      metricsWriter.writeSustentacaoAnalysis(person.slug, analysis)
      log.info('sustentacao:run-analysis análise persistida', { slug: person.slug, assigneeKey })
    } catch (err) {
      log.warn('sustentacao:run-analysis falha ao persistir (graceful)', {
        slug: person.slug,
        error: err instanceof Error ? err.message : String(err),
      })
    }
  }

  return { analysis }
})
```

**Nota ao executor sobre runClaude:** Verificar o padrão exato de como outros handlers chamam o Claude CLI. Em `ai:generate-agenda` provavelmente existe algo como `await runClaudePrompt(prompt)` ou `ClaudeRunner.run(prompt)`. Usar o mesmo mecanismo — não inventar novo. Se o mecanismo usa `settings.claudeBinPath`, garantir que está sendo passado corretamente.

**Nota sobre PersonRegistry.list:** Verificar a assinatura de PersonRegistry.list (pode ser `.list()` ou `.list(workspacePath)`). Usar conforme já está sendo usado em outros handlers do index.ts.
  </action>
  <verify>
    <automated>grep -n "sustentacao:run-analysis\|buildSustentacaoPrompt\|writeSustentacaoAnalysis" /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit/src/main/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - index.ts contém `ipcMain.handle('sustentacao:run-analysis',`
    - index.ts contém `import { buildSustentacaoPrompt }`
    - index.ts contém `buildSustentacaoPrompt(snapshot)`
    - index.ts contém `metricsWriter.writeSustentacaoAnalysis(`
    - index.ts contém `SIGNIFICANT_TICKET_THRESHOLD` ou valor >= 3 no filtro de assignees
    - index.ts contém bloco try/catch em torno de writeSustentacaoAnalysis (graceful degradation)
    - Handler retorna `{ analysis: string }` em caso de sucesso
    - Handler retorna `{ error: string }` em caso de falha de configuração (sem lançar exceção)
  </acceptance_criteria>
  <done>IPC handler sustentacao:run-analysis completo — análise de IA flui para metricas.md dos assignees com tickets significativos</done>
</task>

</tasks>

<verification>
- `grep -c "sustentacao:run-analysis\|buildSustentacaoPrompt\|writeSustentacaoAnalysis" src/main/index.ts` retorna >= 3
- `test -f src/main/prompts/sustentacao-analysis.prompt.ts` retorna 0
- `grep "SUSTENTACAO_ANALISE" src/main/external/MetricsWriter.ts` retorna match
</verification>

<success_criteria>
- Botão "Analisar" na SustentacaoView dispara análise completa via Claude CLI
- Análise inclui padrões, oportunidades de automação, causa raiz e sugestões de SLA
- metricas.md de assignees com >= 3 tickets recebe bloco SUSTENTACAO:ANALISE persistido
- Falha na persistência não impede retorno do resultado ao frontend
- Análise fica disponível no metricas.md para ser lida durante ingestão de artefatos da pessoa
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/999.5-visibilidade-board-sustentacao/999.5-06-SUMMARY.md`
</output>
