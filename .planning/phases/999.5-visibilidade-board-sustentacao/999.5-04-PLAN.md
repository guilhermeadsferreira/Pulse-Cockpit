---
phase: 999.5-visibilidade-board-sustentacao
plan: 04
type: execute
wave: 3
depends_on:
  - 999.5-01
  - 999.5-02
files_modified:
  - src/main/index.ts
  - src/preload/index.ts
autonomous: true
requirements:
  - SUST-01
  - SUST-03

must_haves:
  truths:
    - "window.api.sustentacao.getData() chama IPC e retorna SupportBoardSnapshot | null"
    - "window.api.sustentacao.refresh() força novo fetch do board de sustentação"
    - "IPC handler sustentacao:get-data guarda resultado em cache em disco por 1h (padrão ExternalDataPass)"
    - "Se jiraSupportProjectKey não configurado, handler retorna null sem erro"
  artifacts:
    - path: "src/main/index.ts"
      provides: "IPC handlers sustentacao:get-data e sustentacao:refresh"
      contains: "sustentacao:get-data"
    - path: "src/preload/index.ts"
      provides: "window.api.sustentacao namespace"
      contains: "sustentacao:"
  key_links:
    - from: "src/main/index.ts"
      to: "src/main/external/SupportBoardClient.ts"
      via: "fetchSupportBoardMetrics chamado no IPC handler"
      pattern: "fetchSupportBoardMetrics"
    - from: "src/preload/index.ts"
      to: "src/main/index.ts"
      via: "ipcRenderer.invoke('sustentacao:get-data') → ipcMain.handle('sustentacao:get-data')"
      pattern: "sustentacao:get-data"
---

<objective>
Registrar IPC handlers para o módulo de sustentação em index.ts e expor o namespace window.api.sustentacao no preload. Inclui cache em disco (1h TTL) para evitar chamadas desnecessárias ao Jira.

Purpose: Conecta o SupportBoardClient ao frontend — sem esses handlers, a SustentacaoView (Plan 05) não tem como buscar dados.
Output: index.ts com handlers sustentacao:get-data e sustentacao:refresh; preload com api.sustentacao.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.5-visibilidade-board-sustentacao/999.5-CONTEXT.md
@.planning/phases/999.5-visibilidade-board-sustentacao/999.5-01-SUMMARY.md
@.planning/phases/999.5-visibilidade-board-sustentacao/999.5-02-SUMMARY.md
</context>

<interfaces>
<!-- Contratos que este plano consome -->

De src/main/external/SupportBoardClient.ts (criado no Plan 02):
```typescript
export interface SupportBoardInput {
  config: JiraConfig
  projectKey: string
  slaThresholds?: Record<string, number>
}
export async function fetchSupportBoardMetrics(input: SupportBoardInput): Promise<SupportBoardSnapshot>
```

De src/main/registry/SettingsManager.ts:
```typescript
SettingsManager.load(): AppSettings
// AppSettings agora inclui:
//   jiraSupportBoardId?: number
//   jiraSupportProjectKey?: string
//   jiraSlaThresholds?: Record<string, number>
//   jiraBaseUrl?: string, jiraEmail?: string, jiraApiToken?: string
```

De src/renderer/src/types/ipc.ts (Plan 01):
```typescript
export interface SupportBoardSnapshot { ... }
```

Padrão de cache de ExternalDataPass (referência — não importar a classe, apenas seguir o padrão):
```typescript
// Cache em disco: join(workspacePath, '..', 'cache', 'external', `${slug}.json`)
// Formato: { data: T, fetchedAt: number }
// TTL: 60 * 60 * 1000 (1h)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar IPC handlers sustentacao:get-data e sustentacao:refresh em index.ts</name>
  <files>src/main/index.ts</files>
  <read_first>
    - src/main/index.ts (ler a seção de handlers `external:*` — especialmente external:get-data e external:refresh-person — para entender o padrão de cache em disco)
    - src/main/external/ExternalDataPass.ts (padrão de cache: cacheDir, CACHE_TTL_MS, leitura/escrita de JSON)
    - src/main/external/SupportBoardClient.ts (fetchSupportBoardMetrics, SupportBoardInput)
  </read_first>
  <action>
Adicionar os seguintes imports no topo de index.ts (após os imports de JiraMetrics/GitHubMetrics ou junto com outros imports de external/):
```typescript
import { fetchSupportBoardMetrics } from './external/SupportBoardClient'
import type { SupportBoardSnapshot } from '../renderer/src/types/ipc'
```

Adicionar dois IPC handlers no mesmo bloco dos handlers `external:*` (antes do fechamento da função createWindow ou antes de `ipcMain.handle('shell:open')`):

**Handler sustentacao:get-data:**
```typescript
ipcMain.handle('sustentacao:get-data', async (): Promise<SupportBoardSnapshot | null> => {
  const settings = SettingsManager.load()
  const { jiraSupportProjectKey, jiraBaseUrl, jiraEmail, jiraApiToken, jiraSlaThresholds } = settings

  if (!jiraSupportProjectKey || !jiraBaseUrl || !jiraEmail || !jiraApiToken) {
    return null
  }

  // Cache em disco — padrão ExternalDataPass
  const cacheDir = join(settings.workspacePath, '..', 'cache', 'sustentacao')
  const cacheFile = join(cacheDir, 'board.json')
  const CACHE_TTL_MS = 60 * 60 * 1000

  try {
    if (existsSync(cacheFile)) {
      const cached = JSON.parse(readFileSync(cacheFile, 'utf-8')) as { data: SupportBoardSnapshot; fetchedAt: number }
      if (Date.now() - cached.fetchedAt < CACHE_TTL_MS) {
        log.info('sustentacao:get-data cache hit')
        return cached.data
      }
    }
  } catch { /* cache inválido — refetch */ }

  log.info('sustentacao:get-data buscando board', { projectKey: jiraSupportProjectKey })

  const data = await fetchSupportBoardMetrics({
    config: { baseUrl: jiraBaseUrl, email: jiraEmail, apiToken: jiraApiToken },
    projectKey: jiraSupportProjectKey,
    slaThresholds: jiraSlaThresholds,
  })

  try {
    if (!existsSync(cacheDir)) mkdirSync(cacheDir, { recursive: true })
    writeFileSync(cacheFile, JSON.stringify({ data, fetchedAt: Date.now() }), 'utf-8')
  } catch (err) {
    log.warn('sustentacao:get-data falha ao gravar cache', { error: err instanceof Error ? err.message : String(err) })
  }

  return data
})
```

**Handler sustentacao:refresh:**
```typescript
ipcMain.handle('sustentacao:refresh', async (): Promise<SupportBoardSnapshot | null> => {
  const settings = SettingsManager.load()
  const cacheFile = join(settings.workspacePath, '..', 'cache', 'sustentacao', 'board.json')

  // Invalidar cache antes de refetch
  try {
    if (existsSync(cacheFile)) {
      writeFileSync(cacheFile, JSON.stringify({ data: null, fetchedAt: 0 }), 'utf-8')
    }
  } catch { /* ignore */ }

  // Delegar ao handler principal
  return ipcMain.emit('sustentacao:get-data', null)
})
```

**Nota:** Para `sustentacao:refresh`, em vez de duplicar a lógica, verificar se no projeto existe um padrão de reutilização de handler (ex: chamar a mesma função auxiliar). Se não houver, extrair a lógica de fetch para uma função privada `fetchAndCacheSustentacao()` e chamar de ambos os handlers. O importante é: refresh limpa cache e chama o fetch novamente.

**Imports adicionais necessários em index.ts** (verificar se já importados):
- `existsSync`, `readFileSync`, `writeFileSync`, `mkdirSync` de `'fs'`
- `join` de `'path'`
  </action>
  <verify>
    <automated>grep -n "sustentacao:get-data\|sustentacao:refresh\|fetchSupportBoardMetrics" /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit/src/main/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - index.ts contém `ipcMain.handle('sustentacao:get-data',`
    - index.ts contém `ipcMain.handle('sustentacao:refresh',`
    - index.ts contém `import { fetchSupportBoardMetrics }`
    - index.ts contém `jiraSupportProjectKey` na verificação de configuração (guard clause)
    - index.ts contém `cacheDir` com `'cache', 'sustentacao'` no path
    - index.ts contém `CACHE_TTL_MS` aplicado ao cache
    - Handler retorna `null` quando `jiraSupportProjectKey` não configurado (sem lançar exceção)
  </acceptance_criteria>
  <done>IPC handlers de sustentação registrados em index.ts com cache em disco</done>
</task>

<task type="auto">
  <name>Task 2: Expor namespace api.sustentacao no preload</name>
  <files>src/preload/index.ts</files>
  <read_first>
    - src/preload/index.ts (ler inteiro — padrão de namespaces como external, actions, settings)
  </read_first>
  <action>
Adicionar o namespace `sustentacao` no objeto exposto pelo contextBridge em `src/preload/index.ts`. Inserir após o namespace `external` (para agrupamento lógico):

```typescript
  sustentacao: {
    getData:    () => ipcRenderer.invoke('sustentacao:get-data'),
    refresh:    () => ipcRenderer.invoke('sustentacao:refresh'),
    runAnalysis: () => ipcRenderer.invoke('sustentacao:run-analysis'),
  },
```

**Nota:** `runAnalysis` é adicionado aqui já prevendo o Plan 06 (prompt de IA). O handler `sustentacao:run-analysis` será criado no Plan 06 — por ora o invoke pode retornar erro se chamado, mas o tipo no preload já fica correto.
  </action>
  <verify>
    <automated>grep -n "sustentacao:" /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit/src/preload/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - preload/index.ts contém `sustentacao: {`
    - preload/index.ts contém `getData:` com `invoke('sustentacao:get-data')`
    - preload/index.ts contém `refresh:` com `invoke('sustentacao:refresh')`
    - preload/index.ts contém `runAnalysis:` com `invoke('sustentacao:run-analysis')`
    - O namespace está dentro do objeto passado a `contextBridge.exposeInMainWorld('api', {`
  </acceptance_criteria>
  <done>window.api.sustentacao disponível no renderer com getData, refresh e runAnalysis</done>
</task>

</tasks>

<verification>
- `grep -c "sustentacao:" src/main/index.ts` retorna >= 2
- `grep -c "sustentacao" src/preload/index.ts` retorna >= 4
</verification>

<success_criteria>
- window.api.sustentacao.getData() invoca sustentacao:get-data e retorna SupportBoardSnapshot | null
- window.api.sustentacao.refresh() invalida cache e refetch
- Cache em disco com TTL de 1h — evita chamadas redundantes ao Jira
- Guard clause retorna null sem erro quando board não configurado
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/999.5-visibilidade-board-sustentacao/999.5-04-SUMMARY.md`
</output>
