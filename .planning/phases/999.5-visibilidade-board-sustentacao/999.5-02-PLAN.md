---
phase: 999.5-visibilidade-board-sustentacao
plan: 02
type: execute
wave: 2
depends_on:
  - 999.5-01
files_modified:
  - src/main/external/SupportBoardClient.ts
autonomous: true
requirements:
  - SUST-01

must_haves:
  truths:
    - "SupportBoardClient busca todos os tickets do projectKey configurado (sem filtro de assignee)"
    - "Tickets com idade > threshold do seu tipo são marcados como slaBreached=true (default 5 dias se tipo não configurado)"
    - "Tickets em breach recebem os últimos 3 comentários via JiraClient.getIssueComments"
    - "Resultado retorna SupportBoardSnapshot com topTipos, topLabels, porAssignee e ticketsEmBreach"
  artifacts:
    - path: "src/main/external/SupportBoardClient.ts"
      provides: "fetchSupportBoardMetrics(input) → SupportBoardSnapshot"
      exports: ["fetchSupportBoardMetrics", "SupportBoardInput"]
  key_links:
    - from: "src/main/external/SupportBoardClient.ts"
      to: "src/main/external/JiraClient.ts"
      via: "instância de JiraClient com searchIssuesByAssignee + getIssueComments"
      pattern: "new JiraClient"
    - from: "src/main/external/SupportBoardClient.ts"
      to: "src/renderer/src/types/ipc.ts"
      via: "importa SupportBoardSnapshot e SupportTicket de ipc.ts"
      pattern: "SupportBoardSnapshot"
---

<objective>
Criar SupportBoardClient.ts — camada de dados do board de sustentação. Busca todos os tickets do projeto Jira de suporte, calcula SLA breach por tipo, agrega métricas de volume/tipos/labels/assignee e busca comentários recentes nos tickets em breach.

Purpose: Isola toda a lógica de fetch e cálculo do board de sustentação — o IPC handler (Plan 04) apenas chama fetchSupportBoardMetrics e repassa o snapshot para o frontend.
Output: src/main/external/SupportBoardClient.ts com fetchSupportBoardMetrics exportada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.5-visibilidade-board-sustentacao/999.5-CONTEXT.md
@.planning/phases/999.5-visibilidade-board-sustentacao/999.5-01-SUMMARY.md
</context>

<interfaces>
<!-- Contratos que este plano deve respeitar. Extraídos do codebase. -->

De src/main/external/JiraClient.ts:
```typescript
export interface JiraConfig {
  baseUrl: string
  email: string
  apiToken: string
  projectKey?: string
  boardId?: number
}

export interface JiraIssue {
  key: string
  summary: string
  status: string
  statusCategory: 'new' | 'indeterminate' | 'done'
  type: string
  priority: string
  assignee: string | null
  created: string
  updated: string
  resolved: string | null
  storyPoints: number | null
  labels: string[]
  sprint: string | null
  sprintId: number | null
}

export interface JiraComment {
  id: string
  author: string
  body: string
  created: string
  updated: string
}

class JiraClient {
  constructor(config: JiraConfig)
  // Usar com JQL customizado para buscar por projectKey sem filtro de assignee:
  async searchIssuesByAssignee(assignee: string, jql?: string): Promise<JiraIssue[]>
  // Já implementado — usar para tickets em breach:
  async getIssueComments(issueKey: string, maxComments?: number): Promise<JiraComment[]>
}
```

De src/renderer/src/types/ipc.ts (adicionados no Plan 01):
```typescript
export interface SupportTicket {
  key: string
  summary: string
  type: string
  labels: string[]
  assignee: string | null
  ageDias: number
  status: string
  slaBreached: boolean
  recentComments: Array<{ author: string; body: string; created: string }>
}

export interface SupportBoardSnapshot {
  atualizadoEm: string
  ticketsAbertos: number
  ticketsFechadosUltimos30d: number
  topTipos: Array<{ tipo: string; count: number }>
  topLabels: Array<{ label: string; count: number }>
  ticketsEmBreach: SupportTicket[]
  porAssignee: Record<string, number>
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Criar SupportBoardClient.ts com fetchSupportBoardMetrics</name>
  <files>src/main/external/SupportBoardClient.ts</files>
  <read_first>
    - src/main/external/JiraClient.ts (ler inteiro — especialmente searchIssuesByAssignee, getIssueComments, JiraConfig, JiraIssue, JiraComment)
    - src/main/external/JiraMetrics.ts (referência estrutural — padrão de fetchJiraMetrics)
    - src/renderer/src/types/ipc.ts (SupportTicket e SupportBoardSnapshot adicionados no Plan 01)
  </read_first>
  <action>
Criar o arquivo `src/main/external/SupportBoardClient.ts` com o seguinte conteúdo:

```typescript
import { JiraClient, type JiraConfig } from './JiraClient'
import { Logger } from '../logging/Logger'
import type { SupportBoardSnapshot, SupportTicket } from '../../renderer/src/types/ipc'

const log = Logger.getInstance().child('SupportBoardClient')

const DEFAULT_SLA_DIAS = 5
const BREACH_COMMENTS_MAX = 3
const THIRTY_DAYS_MS = 30 * 24 * 60 * 60 * 1000
const TOP_N = 5

export interface SupportBoardInput {
  config: JiraConfig
  projectKey: string
  slaThresholds?: Record<string, number>
}

function ageDias(createdAt: string): number {
  return Math.floor((Date.now() - new Date(createdAt).getTime()) / (1000 * 60 * 60 * 24))
}

function topN(counts: Record<string, number>, n: number): Array<{ key: string; count: number }> {
  return Object.entries(counts)
    .sort((a, b) => b[1] - a[1])
    .slice(0, n)
    .map(([key, count]) => ({ key, count }))
}

export async function fetchSupportBoardMetrics(input: SupportBoardInput): Promise<SupportBoardSnapshot> {
  const { config, projectKey, slaThresholds = {} } = input
  const client = new JiraClient(config)

  // JQL sem filtro de assignee — busca todos os tickets do projeto
  const jql = `project = "${projectKey}" AND created >= -90d ORDER BY created DESC`

  log.info('SupportBoardClient: buscando tickets', { projectKey })

  const issues = await client.searchIssuesByAssignee('', jql)

  log.info('SupportBoardClient: resultado', { projectKey, totalIssues: issues.length })

  const now = Date.now()
  const thirtyDaysAgo = now - THIRTY_DAYS_MS

  const abertos = issues.filter((i) => i.statusCategory !== 'done')
  const fechados30d = issues.filter(
    (i) => i.statusCategory === 'done' && i.resolved && new Date(i.resolved).getTime() >= thirtyDaysAgo
  )

  // Contagens por tipo e label (somente tickets abertos)
  const tipoCounts: Record<string, number> = {}
  const labelCounts: Record<string, number> = {}
  const assigneeCounts: Record<string, number> = {}

  for (const issue of abertos) {
    tipoCounts[issue.type] = (tipoCounts[issue.type] ?? 0) + 1

    for (const label of issue.labels) {
      labelCounts[label] = (labelCounts[label] ?? 0) + 1
    }

    const key = issue.assignee ?? 'sem_assignee'
    assigneeCounts[key] = (assigneeCounts[key] ?? 0) + 1
  }

  // Detectar tickets em SLA breach
  const breachedIssues = abertos.filter((i) => {
    const threshold = slaThresholds[i.type] ?? DEFAULT_SLA_DIAS
    return ageDias(i.created) > threshold
  })

  // Buscar comentários recentes para cada ticket em breach
  const ticketsEmBreach: SupportTicket[] = []
  for (const issue of breachedIssues) {
    let recentComments: Array<{ author: string; body: string; created: string }> = []
    try {
      const comments = await client.getIssueComments(issue.key, BREACH_COMMENTS_MAX)
      recentComments = comments.map((c) => ({ author: c.author, body: c.body, created: c.created }))
    } catch (err) {
      log.warn('SupportBoardClient: falha ao buscar comentários (graceful)', {
        issueKey: issue.key,
        error: err instanceof Error ? err.message : String(err),
      })
    }

    ticketsEmBreach.push({
      key: issue.key,
      summary: issue.summary,
      type: issue.type,
      labels: issue.labels,
      assignee: issue.assignee,
      ageDias: ageDias(issue.created),
      status: issue.status,
      slaBreached: true,
      recentComments,
    })
  }

  const topTipos = topN(tipoCounts, TOP_N).map(({ key, count }) => ({ tipo: key, count }))
  const topLabels = topN(labelCounts, TOP_N).map(({ key, count }) => ({ label: key, count }))

  return {
    atualizadoEm: new Date().toISOString(),
    ticketsAbertos: abertos.length,
    ticketsFechadosUltimos30d: fechados30d.length,
    topTipos,
    topLabels,
    ticketsEmBreach,
    porAssignee: assigneeCounts,
  }
}
```

**Nota sobre o JQL:** `searchIssuesByAssignee` aceita um JQL customizado como segundo argumento. Passar string vazia como `assignee` é seguro — o JQL customizado sobrescreve a query padrão. Verificar no JiraClient.ts se a assinatura suporta esse uso (o segundo parâmetro `jql?` sobrescreve a query default quando presente).
  </action>
  <verify>
    <automated>test -f /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit/src/main/external/SupportBoardClient.ts && grep -n "fetchSupportBoardMetrics\|SupportBoardInput\|SupportBoardSnapshot" /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit/src/main/external/SupportBoardClient.ts</automated>
  </verify>
  <acceptance_criteria>
    - SupportBoardClient.ts existe em src/main/external/
    - Arquivo exporta `fetchSupportBoardMetrics` e `SupportBoardInput`
    - Arquivo contém `import type { SupportBoardSnapshot, SupportTicket }` de ipc.ts
    - Arquivo usa `new JiraClient(config)` e chama `searchIssuesByAssignee` com JQL customizado
    - Arquivo usa `client.getIssueComments(issue.key, 3)` para tickets em breach
    - Arquivo contém bloco `try/catch` em torno de getIssueComments (graceful degradation)
    - Arquivo calcula `ageDias` a partir de `issue.created`
    - Arquivo usa `slaThresholds[i.type] ?? DEFAULT_SLA_DIAS` (default 5 dias quando tipo não configurado)
    - `ticketsEmBreach` contém apenas tickets de `abertos` (statusCategory !== 'done')
  </acceptance_criteria>
  <done>SupportBoardClient.ts criado e funcional — pronto para ser invocado pelo IPC handler do Plan 04</done>
</task>

</tasks>

<verification>
- `test -f src/main/external/SupportBoardClient.ts` retorna 0
- `grep -c "export" src/main/external/SupportBoardClient.ts` retorna >= 2 (fetchSupportBoardMetrics + SupportBoardInput)
- `grep "getIssueComments" src/main/external/SupportBoardClient.ts` confirma uso para comentários
</verification>

<success_criteria>
- SupportBoardClient.ts criado com fetchSupportBoardMetrics(input: SupportBoardInput): Promise<SupportBoardSnapshot>
- SLA breach calculado por tipo com fallback de 5 dias
- Comentários buscados para tickets em breach com graceful degradation
- Métricas de topTipos, topLabels, porAssignee calculadas a partir de tickets abertos
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/999.5-visibilidade-board-sustentacao/999.5-02-SUMMARY.md`
</output>
